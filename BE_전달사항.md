# BE 전달사항

**2026-08-27 오전 갱신 (3차).** 남아 있던 항목을 **하나씩 실서버로 다시 확인**했고,
그 사이 서버에서 고쳐진 것 둘을 §0-2 에 적었습니다. 적힌 상태코드·응답 키·필드
이름은 모두 **실제 응답**입니다. 추측으로 적은 줄은 없습니다.

```
확인 계정   realsqa0826@test.co.kr   (user 25)
가게        스타벅스 한국프레스센터점 (store 21) · 농민백암순대 본점 (store 32)
```

---

## 0. 한눈에

| | 항목 | 한 줄 |
|---|---|---|
| ✅ | ~~14.1 편집 실패~~ **철회** | 저희 테스트 파일이 원인이었습니다. 실제 촬영본으로는 345초에 완성 (§1-0) |
| 🔴 | **`stage`·`error_message` 가 배포에 안 올라가 있습니다** | 레포에는 이미 있습니다. 배포만 해 주시면 됩니다 (§1-5) |
| 🔴 | **끝나지 않는 편집을 확정해 줄 곳이 없습니다** | AI가 안 끝내면 아무도 안 끝냅니다 — 요금이 계속 나갑니다 (§1-6) |
| 🔴 | **편집이 끝나도 `shorts_status` 가 `DRAFT` 로 남음** | 완성된 프로젝트가 "만들던 영상" 에 남아 **촬영이 막혔습니다** (§1-8) |
| 🟡 | 촬영본 **썸네일 URL 이 없습니다** | 하단 클립 줄에 그림을 못 깔니다. 완성본엔 있는 값입니다 (§1-9) |
| 🔴 | **메뉴 수집이 카카오 후보에서만 됩니다** | 네이버로 고르면 0건. 검색 상위 5건이 네이버입니다 (§1-1) |
| ✅ | ~~5.1 포맷이 3건뿐~~ | **5건으로 늘었습니다. 앱 수정 없이 그대로 뜹니다** (§1-2) |
| ✅ | ~~참고 영상 `start_ms`·`end_ms`~~ | **값이 들어왔습니다. 구간 반복 붙였습니다** (§2-1) |
| 🔴 | **16.1 `sns_account_name` — 인스타는 내부 id, 유튜브는 아예 빈 값** | 사장님 화면에 "계정 이름 없음" 이 뜹니다 (§2-6) |
| 🔴 | **유튜브를 연동해도 16.1 목록에 안 들어옵니다** | 사장님이 연동을 마쳤는데 앱은 계속 "연동하기" 입니다 (§2-7) |
| 🟡 | **`guide_type: DANCE` 가 한 번도 안 옴** | 안무 전용 화면이 도달 불가입니다 (§2-2) |
| 🟡 | **매장 정보 갈아끼기 — 3.1 필드·재가져오기** | 시안 8차 재검색 화면을 못 만들고 있습니다 (§2-4 · §2-5) |
| 🟡 | 3.6 로고 캐시 두 가지 | 아직 못 재봤습니다 (§2-3) |
| ⚪ | 푸시 — **계약 확정 · 앱 붙임 완료** | 남은 건 payload 키 이름 하나입니다 (§3) |
| ⚪ | 유료 플랜 MVP 범위 · 기존 대기 항목 | §4 |

어제 올렸던 편집 실패는 **저희 테스트 파일 문제로 확인돼 철회했습니다**(§1-0).
실제 촬영본으로는 345초 만에 완성됩니다. 인스타 연동도 풀렸습니다(§0-2).

**2026-08-28 에 다섯 건을 새로 올립니다** — §1-8 `shorts_status` · §1-9 촬영본 썸네일 ·
§2-4 3.1 필드 · §2-5 매장 재가져오기 · §2-6 SNS 계정 이름입니다. §2-4·§2-5 는 한 묶음으로,
답을 받아야 시안 8차의 **매장 재검색 화면**을 만들 수 있습니다.
§1-8 은 사장님이 **새 영상을 못 찍는** 증상으로 나타나 급합니다. 앱에서 우회는 해 뒀지만
두 응답(4.3 목록 · 15.2 완성본)이 서로 어긋나는 상태 자체는 남아 있습니다.

> ### 2026-08-28 저녁 — 배포해 주신 뒤 저희가 열어 둔 항목을 전부 다시 재봤습니다
>
> | 항목 | 결과 |
> |---|---|
> | §1-2 5.1 포맷 | ✅ **3건 → 5건.** 앱 수정 없이 그대로 뜹니다 |
> | §2-1 참고 영상 구간 | ✅ **값이 들어왔습니다.** 구간 반복 붙였습니다 |
> | §1-1 2.3 가져오기 | ❌ **메뉴 0건** 그대로입니다 (사진은 2건 들어옵니다) |
> | §2-2 `guide_type: DANCE` | ❌ 새 포맷(55)으로도 **전부 OVERLAY** |
> | §2-1 정보형 구간 | ❌ 정보형은 `start_ms`·`end_ms` 가 아직 `null` — 의도인지만 알려주세요 |
> | §2-4·§2-5 매장 재검색 | ❌ 관련 경로가 아직 없습니다 (전체 52개 확인) |

---

## 0-1. ✅ 이번 회신으로 닫힌 것 — **보내주신 일곱 항목을 전부 실서버로 재확인했습니다**

| 항목 | 저희가 확인한 결과 | 앱 쪽 |
|---|---|---|
| **2.1 지번 주소** | ✅ 다섯 키워드 **98건 전부** 채워집니다. NAVER 후보도 옵니다 | 확인 시트에 줄을 넣었습니다 |
| **2.1 `kakao_place_id`** | ✅ **KAKAO 소스 후보에만** 옵니다(20건 중 15건) | 등록 때 되돌려 보내도록 **고쳤습니다** — 아래 ⚠️ |
| **푸시 토큰 필드명** | ✅ `push_token` 200 · `token` 은 422 | 그대로 맞추겠습니다 |
| **토큰 upsert** | ✅ 같은 값을 두 번 보내도 200 | **매번 보내는 쪽**으로 정했습니다 |
| **토큰 삭제 API** | ✅ 불필요로 확인 | 안 만듭니다 |
| **15.1 `track.mode`** | ✅ 해석이 맞다고 확인 주셨습니다 | 화면 그대로 둡니다 |
| **6.1·6.2·6.3 폐기** | ✅ `quiz-questions` 404 재확인 | **흉내 장치를 걷어냈습니다**(api 층 7개 파일) |
| **3.1 명세 Body** | ✅ 서버가 맞고 문서만 갱신 | 코드 그대로 둡니다 |

**부수적으로 닫힌 것 — 전화번호(옛 Q2-1b).** "`phone` 이 자주 `null`" 이라고 적어
드렸는데, 다시 세어 보니 **KAKAO 후보 15건은 전부 번호가 있고 NAVER 후보 5건만
`null`** 이었습니다. 저희가 상위 5건만 보고 판단한 것입니다 — **따로 손보실 것 없습니다.**

> ### ⚠️ 그리고 저희 쪽 잘못 하나를 정정합니다 — `kakao_place_id`
>
> 지난 문서에 "프론트가 2.2 등록 때 그대로 돌려보내도록 만들어 뒀는데 받을 값이
> 없다" 고 적었습니다. **틀렸습니다.** 타입에는 선언해 뒀지만 **실제로 실어 보내는
> 줄이 빠져 있었습니다**(매장 등록을 2단계로 나눌 때 누락된 것으로 보입니다).
>
> 그동안 서버도 값을 안 내려줘서 아무도 몰랐고, 배포해 주시면서 드러났습니다.
> **오늘 고쳤습니다.** 다음 배포부터는 KAKAO 후보로 등록하면 `kakao_place_id` 가
> 실제로 함께 갑니다 — §1-1 과 직접 얽히는 값이라 아래에 이어 적습니다.

---

## 0-2. ✅ 그 뒤로 **서버에서 고쳐진 것 둘** — 오늘 아침 확인했습니다

회신에는 없었지만 서버가 바뀌어 있었습니다. 저희가 먼저 발견해 적습니다.

### ① 16.1 인스타 `client_id` — **바뀌었고, 이제 로그인 화면이 열립니다** 🎉

가장 크게 막고 있던 것입니다.

```
전    client_id=1541622377157897   ← 페이스북 App ID. "Invalid scope" 로 막힘
지금  client_id=1075306164984921   ← 인스타그램 App ID

GET /sns-connections/authorize?platform=INSTAGRAM  → 200
  https://www.instagram.com/oauth/authorize?client_id=1075306164984921
    &redirect_uri=https://sarils.p-e.kr/sns-connections/callback
    &response_type=code&scope=instagram_business_basic instagram_business_manage_insights

그 주소를 실제로 열어 봤습니다 (모바일 UA)
  → 302  /oauth/authorize/third_party/…      ← 정상 동의 흐름
  → 200  로그인 폼 있음 · "Invalid scope" 없음 · error_code 없음
```

**앱은 손댈 것이 없습니다.** 지금 깔린 APK 로도 그대로 동작합니다.

> ⚠️ **남은 확인 하나 — 실제 로그인은 사장님 계정으로만 됩니다.** 저희에게 인스타
> 비즈니스 계정이 없어서 로그인 화면까지만 확인했습니다. 로그인 → 콜백 → 토큰 교환이
> 끝까지 도는지 **한 번만 눌러 봐 주시면** 완전히 닫힙니다. 실패하면 그 화면 문구를
> 그대로 알려주세요 — 앱은 이제 실패를 정중앙 얼럿으로 보여줍니다.

### ② R06 추천에 `video_format_id` 가 **들어왔습니다**

```
명세 스키마  RecommendationResponse → …, video_format_id     ← 추가됨
실제 응답    대화를 끝까지 태워 확인 (세션 159 · 9턴)

{ "recommendation_id": "rec_010e5e9d…", "title": "빠른 전개로 보는 딸기 크림 라떼 릴스",
  "concept": "…", "editing_template_id": "gt_cafe_recommendation",
  "editing_template_version": 3, "video_format_id": 45 }      ← 값이 실제로 옵니다
```

요청드린 그대로입니다. 감사합니다 — 이제 프론트가 5.2 로 **썸네일·촬영시간·난이도·
얼굴노출**을 스스로 채울 수 있습니다. 시안의 추천 카드에서 비워 뒀던 **해시태그 3개와
숏츠 영상**이 그 자리에 들어갑니다. **앱 쪽 연결은 저희가 합니다**(화면 코드라
무선 배포됩니다).

---

## 1. 🔴 지금 막고 있는 것 둘 (+ 철회 1건)

### 1-0. ✅ **정정 — 편집은 정상입니다. 저희 테스트 촬영본이 문제였습니다** (2026-08-27)

앞서 "14.1 편집이 100% 실패한다" 고 올렸던 것을 **철회합니다.** 8번 실패의 원인은
서버가 아니라 **저희가 올린 테스트 영상 파일**이었습니다. 확인 과정을 그대로 적습니다.

**대조 실험 — 프로젝트 56 에서 촬영본만 바꿨습니다**

| | 촬영본 | 결과 |
|---|---|---|
| output 66 · 69 | 저희가 만든 조각 MP4 | `FAILED` (273초 · 약 6분) |
| **output 71** | **실제 폰 촬영본** | **`COMPLETED` 100% · 345초** |

같은 프로젝트, 같은 장면 구성, 같은 요청입니다. **바뀐 것은 파일뿐입니다.**
완성본을 내려받아 확인했습니다 — 1080×1920 · 9.000초 · 0.84MB, 정상 재생됩니다.

**저희 파일이 왜 문제였는가**

촬영을 흉내 내려고 크롬 `MediaRecorder` 로 만든 파일이었습니다.

```
저희 테스트 파일   mvhd timescale 0 · duration 3870643611 → 길이 불명
                  박스 구조 ftyp moov moof mdat mfra → 조각(fragmented) MP4
                  seekable 범위 0 (브라우저도 "탐색 불가" 로 봅니다)

실제 폰 촬영본     mvhd timescale 1000 · duration 6167 → 6.167초 (길이가 정확히 박혀 있음)
```

길이가 헤더에 없고 탐색이 안 되는 가변프레임 파일이라, 길이 계산도 프레임 추출도
제대로 될 수 없는 입력이었습니다. **`expo-camera` 가 만드는 실제 촬영본은 이렇지 않습니다.**

**보내주신 원인 분석 둘에 대한 회신**

| 분석 | 대상 output | 저희 확인 |
|---|---|---|
| ① 2~4ms 시간 경계 오차 | 59 · 66 · 69 | 전부 **소리 없는** 조각 MP4 를 올린 건입니다 |
| ② 프레임 추출 실패 | 61 · 63 · 65 | 전부 **소리 있는** 조각 MP4 를 올린 건입니다 |

두 그룹이 저희가 올린 **파일 두 종류와 정확히 갈립니다.** 즉 **두 분석 모두 저희
테스트 파일이 원인**이었습니다. 정상 파일로 바꾸자 같은 자리에서 성공했습니다.
헛수고하게 해 드려 죄송합니다.

**그래도 남겨 두시면 좋겠는 것 셋** (급하지 않습니다)

1. **`EditResultResponse` 에 `error_code` · `error_message`** — 지금은 실패해도 앱이
   "편집을 끝내지 못했어요" 밖에 못 말합니다. 이번에도 사유를 못 봐서 BE 로그를
   여쭐 수밖에 없었습니다. 필드가 있었으면 저희가 스스로 알아냈을 일입니다.
2. **FFmpeg stderr 를 버리지 않기** — ②의 마지막 한 단계를 확정 못 하신 이유입니다.
3. **트림 종료 지점 clamp / 1프레임 허용** — ①은 저희 파일이 방아쇠였지만, 재인코딩에서
   몇 ms 줄어드는 건 정상 파일에서도 날 수 있습니다. 방어해 두시면 안전합니다.

**아직 남은 것 — 사장님 실기기 실패는 별개입니다**

사장님이 **실제 폰으로 3초 이내 컷을 여러 개 찍어 만드셨을 때도 실패**하셨습니다.
그건 정상 촬영본이므로 위 원인과 다릅니다. **그때의 `video_output_id` 를 알려주시면**
저희가 바로 대조하겠습니다.

---

### 1-3. 🔴 **메뉴 사진을 붙일 방법이 없습니다** (2026-08-27 신규)

매장 메뉴 관리에서 메뉴에 사진을 붙이는 경로가 **어느 쪽으로도 막혀 있습니다.**
QA 계정(user 25 · store 21)으로 실제 파일을 올려 확인했습니다.

```
POST /stores/21/photos (3.3)  → 201
  file_url 길이 1808자  (서명이 붙은 S3 주소)

PATCH /stores/21/menus/{id}  { "image_url": <그 1808자> }
  → 422  image_url: String should have at most 255 characters

PATCH /stores/21/menus/{id}  { "image_url": <서명 뗀 88자> }
  → 200  저장은 됩니다
GET /stores/21/menus  → image_url 그대로 88자 (서버가 다시 서명해 주지 않습니다)
  그 주소로 이미지 요청 → 403
```

**정리하면**
- 받은 주소를 그대로 넣으면 **255자 제한에 걸려 422**
- 서명을 떼고 넣으면 **저장은 되는데 그 주소가 403** 이라 사진이 안 보입니다

**가게 로고는 잘 됩니다.** 3.1 이 `logo_url` 을 **1806자짜리 서명 주소로 다시 만들어**
주기 때문입니다(실제로 200 으로 이미지가 내려옵니다). **메뉴 사진만 그 처리가 없습니다.**

**부탁드립니다 — 둘 중 하나면 됩니다**
1. 메뉴 조회(3.2)에서도 `image_url` 을 **로고처럼 서명해서** 내려 주기 (앱 수정 불필요)
2. `image_url` 이 **객체 키(88자)** 를 받도록 하고 읽을 때 서명 (앱은 서명만 떼면 됩니다)

앱은 지금 **실패를 사장님께 알리도록** 고쳤습니다(전에는 조용히 넘어가 사진 자리가
그대로 비었습니다). 고쳐 주시면 앱 수정 없이 바로 동작합니다.

### 1-4. 🟡 그 밖에 이번 점검에서 나온 값 두 가지

**① 15.1 `cover_image_url` 이 `null` 입니다** — 완성본(output 71, `COMPLETED`,
1080×1920, 영상 주소 정상)인데도 커버가 없습니다. **마이페이지 3열 그리드가 이 값으로
썸네일을 그립니다.** 지금은 전부 빈칸으로 뜹니다.

**② 7.2 `shooting_summary.expected_duration_sec` 가 1800초(30분)입니다**
```
project 56 · video_format_id 46
  5.2 포맷      expected_duration_sec 13초
  7.2 요약      expected_duration_sec 1800초 · difficulty "상"
  7.2 장면 합   3 + 2 + 4 = 9초
```
13초짜리 포맷의 촬영 준비 화면에 **"촬영 30분"** 으로 찍힙니다. 장면 합(9초)과도
안 맞습니다. 기본값이 그대로 나가는 것 같습니다.

---

### 1-5. 🔴 **`stage` · `error_message` — 코드엔 있는데 배포가 안 됐습니다** (2026-08-27)

레포를 직접 받아 확인했습니다. **이미 만들어 두셨습니다.**

```python
# backend-develop/app/schemas/shorts_project.py:398
class EditResultResponse(BaseSchema):
    video_output_id: int
    render_status: RenderStatus
    progress_percent: int
    stage: str | None = None            # ← 405줄
    preview_video_url: str | None
    timeline_summary: list[TimelineItem]
    missing_scene_roles: list[str] | None = None
    available_options: list[str] | None = None
    error_message: str | None = None    # ← 414줄
```

그런데 **실서버 `openapi.json` 에는 이 둘이 없습니다.**

```
실서버 EditResultResponse
  video_output_id, render_status, progress_percent,
  preview_video_url, timeline_summary, missing_scene_roles, available_options
```

`sync_output()` 도 `render_stage`·`render_progress`·`error_message` 를 매번 갱신하도록
이미 고쳐져 있습니다(`app/services/video_edit.py:196~205`). **배포만 하시면 됩니다.**

이게 올라오면 앱이 두 가지를 바로 할 수 있습니다.
- 진행률(%)에서 단계를 **역산하던 것**을 그만두고 `stage` 를 그대로 씁니다
- 실패했을 때 사장님께 **"다시 시도하면 됩니다" / "촬영본을 다시 올려 주세요"** 를
  구분해 말할 수 있습니다 (지금은 "편집을 끝내지 못했어요" 한 문장뿐입니다)

### 1-6. 🔴 **끝나지 않는 편집을 확정해 줄 곳이 없습니다** (2026-08-27)

**하루 만에 AI 요금이 $18 나왔습니다.** 사장님 화면은 몇 시간째 "편집중" 이었습니다.
AI 레포까지 읽어 경로를 찾았고, 자세한 내용은 **`AI_전달사항.md`** 에 정리해 AI팀에
따로 전달합니다. 요약하면 —

```
AI  editing_orphan_stale_seconds = 900   (15분 이상 RUNNING 이면 고아 판정)
AI  recover_orphaned_editing_runs()      → status=QUEUED 로 되돌리고 재실행
    ⚠️ 시도 횟수 상한이 없습니다 → 15~20분마다 영원히 반복 → 매번 LLM 요금
```

**백엔드는 잘 막고 있습니다** — `start_edit()` 이 진행 중 산출물을 재사용하므로
(`app/services/video_edit.py:122`) 사장님이 "다시 시도" 를 눌러도 새 런이 안 생깁니다.
문제는 **AI 가 스스로 계속 돌린다**는 것이고, 그 사이 `render_status` 는
`PENDING ↔ PROCESSING` 만 오가서 **아무도 이 편집을 끝내지 않습니다.**

`sync_output()` 은 AI 상태를 비추기만 합니다(`still_in_progress` 가 아니면 그대로 반환).
즉 **AI 가 끝내지 않으면 백엔드도 끝내지 않고, 앱도 영원히 기다립니다.**

**부탁드립니다 — 안전장치를 백엔드에 하나 두시면 좋겠습니다**

AI 쪽 상한(§AI 문서 1번)이 근본 해결이지만, 그것과 별개로 백엔드에도 마지막 방어선이
있으면 좋겠습니다.

```
VideoOutput.created_at 기준 N분(예: 40분)이 지나도 PENDING/PROCESSING 이면
  → render_status = FAILED
  → error_message = "편집이 시간 안에 끝나지 않았습니다"
```

이유는 둘입니다.
1. **화면이 사장님께 상황을 말할 수 있습니다.** 지금은 앱이 자체 타이머(15분)로만
   판단하는데, 서버는 여전히 진행 중이라고 하니 둘이 어긋납니다.
2. **편집 완료 푸시가 이 상태에 걸려 있습니다.** 끝나야 발송되는 구조라
   (`app/services/edit_notify.py`), 안 끝나면 알림도 영영 안 옵니다.

### 1-7. 🟡 촬영 가이드에 가게 정보가 전달되지 않습니다 — **원인을 찾았습니다**

§1-0-B 에서 "같은 포맷이면 가게·목적·메뉴와 무관하게 기획이 동일하다" 고 올렸던 건인데,
레포를 보니 **의도된 설계**였습니다.

```python
# app/services/ai_client.py:308
del store, project  # 현재 AI 조회 계약은 템플릿 id와 버전만 받는다.

GET /api/v1/editing-templates/{editing_template_id}/versions/{version}/shooting-guide
```

가게·프로젝트를 받아서 **그 자리에서 버리고** 템플릿 id + 버전만으로 부릅니다.
주석에 인용된 AI팀 지침(`docs/AI_연동_입출력.md` 13번)이 근거로 보입니다.

**그래서 이건 백엔드 버그가 아닙니다.** AI 쪽 계약이 가게 컨텍스트를 받도록 열려야
풀립니다 — `AI_전달사항.md` §4 로 요청했습니다.

다만 **그 계약이 열리면 백엔드 수정은 한 줄입니다** — 이미 `store`·`project` 를 손에
쥔 채 버리고 있으니, 그대로 실어 보내기만 하면 됩니다. 미리 알아 두시면 좋겠습니다.

---

### 1-8. 🔴 **편집이 끝나도 `shorts_status` 가 `DRAFT` 로 남습니다** (2026-08-28 신규)

렌더까지 끝나 완성 영상이 15.2 에 뜨는 프로젝트인데, 4.3 목록에서는 계속
`shorts_status: "DRAFT"` 입니다. `COMPLETED` 로 넘어가는 지점이 없어 보입니다.

**증상 — 사장님이 새 영상을 못 찍었습니다.**
마이페이지의 "만들던 영상" 카드는 `GET /projects?shorts_status=DRAFT` 로 고릅니다.
완성된 프로젝트가 계속 이 목록에 남으니 그 카드가 안 사라지고, 눌러 들어가면
촬영 화면이 "이미 다 찍었어요" 로 막혀서 **다음 작업으로 갈 길이 끊겼습니다.**

```
GET /projects?store_id=...&shorts_status=DRAFT
  → 렌더 끝난 프로젝트가 그대로 들어 있음
GET /stores/{id}/shorts            (15.2)
  → 같은 프로젝트의 완성본이 이미 있음   ← 두 응답이 서로 어긋납니다
```

**요청:** 편집·렌더가 완료되면 해당 프로젝트의 `shorts_status` 를 `COMPLETED` 로
전이시켜 주세요. enum 에 이미 있는 값입니다(`DRAFT | IN_PROGRESS | COMPLETED`).

**앱은 우회해 뒀습니다(2026-08-28).** 15.2 완성 목록이 `shorts_project_id` 를 주므로
**완성본이 있는 프로젝트는 "만들던 영상" 에서 빼고** 고릅니다. 다만 목록을 한 번 더
불러야 판단이 서고 그 응답이 늦으면 잠깐 잘못 뜨므로, **서버가 고쳐 주시는 게 맞습니다.**

---

### 1-9. 🟡 **촬영본 썸네일 URL 이 없습니다** (2026-08-28 신규)

촬영 화면 하단에 찍은 컷을 **작은 그림으로** 늘어놓는 구성으로 시안이 바뀌었습니다.
그런데 촬영본에서 그림을 얻을 데가 없습니다.

```
GET /tasks/{task_id}/footage
  → { task_id, file_url, footage_type }     ← 영상 URL 뿐, 썸네일 없음
```

완성 영상(15.2 `cover_image_url`)에는 이미 표지 이미지를 만들고 계시니,
**촬영본에도 같은 것을 하나 붙여 주시면** 앱은 이미지 한 장만 깔면 됩니다.

**요청:** 8.3 업로드 응답과 `GET /tasks/{id}/footage` 에 `thumbnail_url` 추가.
첫 프레임이면 충분합니다.

**앱 현황(2026-08-28).** 방금 찍어 **기기에 파일이 남아 있는 컷**만 첫 프레임을
그리고, 앱을 다시 켜거나 지난번에 찍은 컷은 색 채움 + 체크로 둡니다.
없는 그림을 지어내지 않습니다.

> 앱에서 영상 URL 로부터 직접 뽑는 방법(`expo-video-thumbnails`)도 있지만
> **네이티브 패키지라 APK 를 새로 구워 배포**해야 하고, 기기마다 컷 수만큼
> 추출 시간이 듭니다. 서버가 한 줄 주시는 쪽이 훨씬 쌉니다.

---

## 1-1~1-2. 🔴 이전부터 막고 있던 것 둘

### 1-0-B. 🟡 7.1 촬영 기획이 포맷 하나로만 정해집니다 — 매장이 반영되지 않습니다

> **2026-08-27 후속: 원인을 찾았습니다 → §1-7.** 백엔드가 가게·프로젝트를 버리고
> 템플릿 id + 버전만으로 AI 를 부르는 구조였습니다(의도된 설계). 아래는 그 전에
> 블랙박스로 확인한 실측 기록이라 근거로 남깁니다.

같은 포맷이면 **어느 가게든, 어떤 목적이든, 메뉴가 있든 없든 장면 문구가 글자 하나까지
똑같습니다.** 사장님이 "구간별로 나눠주는 글이 매번 같은 것 아니냐" 고 하셔서 확인했습니다.

**대조 실험 — 한 번에 하나씩만 바꿨습니다 (전부 `video_format_id: 46`)**

| | 가게 | 목적 | 메뉴 | 결과 |
|---|---|---|---|---|
| project 66 | 21 스타벅스(카페) | 가게소개 | 0개 | 기준 |
| project 67 | 21 스타벅스(카페) | 가게소개 | 0개 | **66과 완전히 동일** |
| project 68 | 21 스타벅스(카페) | 가게소개 | 0개 | **동일** |
| project 69 | **36 순대국집(식당)** | 가게소개 | 0개 | **동일** |
| project 70 | 21 스타벅스 | **메뉴소개** | 0개 | **동일** |
| project 71 | 21 스타벅스 | 메뉴소개 | **3개** (아메리카노·카페라떼·치즈케이크) | **동일** |

`scene_description` · `scene_dialogue` · `target_duration_sec` 이 6개 장면 전부
바이트 단위로 같습니다. **가게·목적·메뉴 중 무엇을 바꿔도 출력이 안 움직입니다.**

**내용도 매장 것이 아닙니다.** 스타벅스(카페) 사장님께 나가는 문구입니다.

```
2) 1.50s~4.50s. 같은 테이블 프레임에서 음료와 장식된 디저트가 즉시 등장한 상태로 …
5) 8.50s~10.00s. 손을 여는 순간 피자 접시가 테이블 위에 즉시 등장한 상태를 …
1) 0.00s~1.50s. 초록색 상의의 여성이 테이블 앞에 앉아 손동작을 만들고 …
```

카페에 **피자**를, 사장님께 **"초록색 상의의 여성"** 을 찍으라고 합니다. 이건 우리 매장의
촬영 기획이 아니라 **참고 영상(원본 트렌드 영상)을 그대로 묘사한 글**로 보입니다.
9.1 촬영 가이드도 같습니다 — "초록색 상의로 앉아 손동작을 만들고, 테이블 한쪽은 비워 둔다".

**포맷마다 고정이되, 시점에 따라 한 번씩 바뀝니다** (= 캐시로 보입니다)

```
08-26 생성 project 49 · 56  (format 46)  장면 3개 · 타임코드 없음
    "첫 번째 손짓 변환 · 디저트와 음료 등장 — 노란 상의 인물이 …"
    → 49 와 56 도 서로 완전히 동일했습니다

08-27 생성 project 62·66·67·68·69·70·71 (format 46)  장면 6개 · 타임코드 있음
    "0.00s~1.50s. 초록색 상의의 여성이 …"
```

같은 포맷인데 **어제 것과 오늘 것이 다릅니다.** 하드코딩이라기보다 **포맷 단위로 한 번
만들어 캐시**하고, 그 사이 프롬프트가 바뀌면서 갱신된 것으로 보입니다.

**확인 부탁드립니다**
1. 7.1 기획을 **포맷 단위로 캐시**하고 계신 게 맞습니까? 그렇다면 캐시 키에
   **가게·목적·메뉴**가 빠져 있습니다.
2. 기획 프롬프트에 **매장 정보(가게명·업종·메뉴·사진·상권분석)** 가 실제로 들어가고
   있습니까? 지금 출력만 보면 참고 영상 묘사만 들어가는 것으로 보입니다.
3. §1-1 메뉴 수집이 `PENDING` 이라 **넣을 매장 정보 자체가 비어 있는 것**이라면,
   그 둘이 같은 뿌리입니다. 다만 **메뉴를 직접 3개 넣은 project 71 도 결과가 같았습니다** —
   메뉴가 있어도 안 쓰입니다.

**왜 중요한가:** 사장님이 받는 촬영 지시가 자기 가게와 무관합니다. 카페 사장님이
피자를 찾게 됩니다. 촬영 화면 하단 문구도 같은 값이라 촬영 내내 노출됩니다.

---

### 1-1-A. 🆕 **메뉴를 어디서 가져오면 되는지 찾았습니다** (2026-08-27)

2.3 메뉴 수집이 계속 `PENDING` 이라, 저희 쪽에서 **실제로 메뉴가 나오는 경로**를 찾아
확인했습니다. **카카오 플레이스 상세에 메뉴가 그대로 들어 있습니다.**

```
GET https://place-api.map.kakao.com/places/panel3/{kakao_place_id}
헤더  Referer: https://place.map.kakao.com/{kakao_place_id}
      Origin:  https://place.map.kakao.com
      pf: web
      User-Agent: (일반 브라우저 UA)
→ 200
```

응답 안 `menu.menus` 가 저희가 필요한 전부입니다.

```
menu.menus.menu_type        "FOOD"
menu.menus.items_updated_at "2026-06-18 16:31:02"
menu.menus.items[]          { product_id, name, price, photo_url, is_recommend, mod_at }
menu.menus.photos[]         메뉴 사진들
```

**실측 4곳** (앱이 2.1 검색에서 이미 받아오는 `kakao_place_id` 로만 조회했습니다)

| 가게 | place_id | 메뉴 | 사진 | 예시 |
|---|---|---|---|---|
| 농민백암순대 시청직영점 | 1062153333 | **7건** | 16장 | 국밥 11,000 · 토종순대 13,000 · 술국 19,000 |
| 농민백암순대 본점 | 17163273 | **7건** | 2장 | 국밥 11,000 · 국밥(정식) 17,000 |
| 농민백암순대 강남직영점 | 16421356 | **6건** | 1장 | 술국 19,000 · 국밥 11,000 |
| 농민백암 | 175712560 | 0건 | 0장 | (카카오에 메뉴가 등록 안 된 가게) |

`name` · `price` · `photo_url` 세 개가 **3.2 메뉴가 요구하는 값과 정확히 같습니다.**
그대로 넣으시면 됩니다.

**중요 — 공식 검색 API 로는 안 나옵니다.** 카카오 로컬 API·네이버 지역검색 API 응답에는
가게명·주소·전화·카테고리·좌표뿐이고 **메뉴 필드가 없습니다.** 2.1 검색 결과만 가지고는
메뉴를 채울 방법이 없어서, 위 상세 조회가 따로 필요합니다.

**앱은 이미 준비돼 있습니다.** 2.2 등록에 `kakao_place_id` 를 실어 보내고 있습니다
(§0-1 에서 확인해 주신 그 값). 서버가 그 값으로 위 주소를 한 번만 부르면 됩니다.

⚠️ 공개 문서가 있는 API 가 아니라 **카카오맵 웹이 쓰는 내부 경로**입니다. 헤더가 없으면
404·406 이 나고(저희도 처음엔 404 였습니다), 카카오가 바꾸면 깨질 수 있습니다.
그러니 **실패해도 등록은 그대로 진행되게** 하고(지금처럼), 메뉴는 사장님이 직접 채울 수
있는 길(3.2)을 계속 열어 두는 게 안전합니다.

### 1-1-B. 새로 등록한 가게도 똑같습니다 (2026-08-27 확인)

`kakao_place_id` 를 실어 **새 가게 두 곳을 등록해** 봤지만 결과가 같습니다.

| 가게 | 출처 | 등록 | 기본정보 | 메뉴 | 사진 | 상권분석 |
|---|---|---|---|---|---|---|
| 36 농민백암순대 시청직영점 | NAVER | 08-27 | SUCCESS | **PENDING** | PENDING | PENDING |
| 37 농민백암순대 강남직영점 | KAKAO | 08-27 | SUCCESS | **PENDING** | PENDING | PENDING |
| 21 스타벅스 한국프레스센터점 | — | 08-26 | SUCCESS | **PENDING** | SUCCESS | PENDING |
| 32 농민백암순대 본점 | — | 08-26 | SUCCESS | **PENDING** | PENDING | PENDING |

**하루가 지난 가게도 `PENDING`** 입니다. 잠깐 걸리는 게 아니라 넘어가지 않습니다.
출처(NAVER/KAKAO)와도 무관합니다.

---

### 1-1. 🔴 **메뉴 수집이 카카오 후보에서만 됩니다** (2026-08-28 재검증)

새 계정·새 매장으로 **앱과 똑같은 본문**을 보내 처음부터 끝까지 재현했습니다.

| 고른 후보 | `kakao_place_id` | 메뉴 |
|---|---|---|
| **KAKAO** 스타벅스 더북한산점 (store 67) | `"76206032"` | **즉시 SUCCESS · 5건** ✅ |
| **NAVER** 스타벅스 한국프레스센터점 (store 68) | `null` | **35초+ PENDING · 0건** ❌ |

```
KAKAO  기본정보=SUCCESS  메뉴=SUCCESS  사진=PENDING  상권분석=PENDING
       → 브루드 커피 4500 · 카푸치노 5200 · 카페 라떼 5200 · 카페 모카 5700 · 아메리카노 4700

NAVER  기본정보=SUCCESS  메뉴=PENDING  사진=PENDING  상권분석=PENDING
       → 0건
```

**메뉴 수집 자체는 고쳐 주셨습니다 — 카카오 후보면 즉시 들어옵니다.** 감사합니다.
그리고 **앱이 `kakao_place_id` 를 제대로 싣는 것도 이걸로 함께 증명됐습니다.**

#### 🔴 Q1-1a. 안전망이 네이버 후보에 안 걸립니다

회신 2번에서 "`kakao_place_id` 가 없어도 이름+좌표로 카카오를 다시 조회한다" 고
하셨는데, **네이버로 등록한 store 68 은 35초가 지나도 `메뉴=PENDING` 이고 0건입니다.**

한국프레스센터점은 상호도 좌표도 정확한 실제 매장이라 100m 조건에 걸려야 합니다.
안전망이 도는지, 아니면 조건이 더 빡빡한지 확인 부탁드립니다.

> **이게 급한 이유** — `GET /stores/search?keyword=스타벅스` 결과의 **상위 5건이 전부
> NAVER** 입니다(한국프레스센터점·무교로점·시청점·무교동점·환구단점). 카카오 후보는
> 6번째부터 나옵니다. **사장님은 위에서 고릅니다.** 지금 상태면 상당수가 메뉴 없이
> 시작하게 됩니다.

#### Q1-1b. 이미 등록된 매장을 다시 수집해 주세요

기존 QA 매장(store 21, NAVER)은 2026-08-27 부터 `메뉴=PENDING` 에 멈춰 있고
앱에서 되돌릴 방법이 없습니다(§2-5). 한 번 돌려 주시거나 재수집 경로를 주세요.

#### Q1-1c. 사진·상권분석은 카카오 후보에서도 `PENDING` 입니다

store 67 은 메뉴가 즉시 들어왔는데 **사진과 상권분석은 76초가 지나도 `PENDING`**
이었습니다. 이 둘도 같이 봐 주세요.

---

### (옛 기록) 2.3 가져오기가 `PENDING` 에서 안 넘어갑니다

```
GET /stores/21/import-status
  overall_status: IN_PROGRESS
  기본정보 SUCCESS / 메뉴 PENDING / 사진 PENDING / 상권분석 PENDING

GET /stores/21/menus             → { "menus": [] }
GET /stores/21/target-customers  → { "target_customers": [] }
GET /stores/21/photos            → { "photos": [] }
```

2.1 검색으로 등록한 **네이버 유명 프랜차이즈 지점**인데도 셋 다 비어 있습니다.
등록 직후부터 하루가 지나도 같습니다 — **2026-08-27 오전에 또 재봤고 그대로입니다.**
이제 이 항목이 **혼자 남은 가장 오래된 미해결**입니다.

**막히는 것:** AI 추천 대화가 "어떤 메뉴를 홍보할까요?" 를 묻는데 고를 것이 없습니다.

> **API 자체는 멀쩡합니다.** 오늘 앱에서 메뉴를 직접 넣고 고치고 지워 봤고
> POST 201 · PATCH 200 · DELETE 200 전부 정상이었습니다. **막힌 것은 수집기 쪽**입니다.
>
> 프론트는 사장님이 직접 넣는 길을 열어 뒀습니다(매장 정보 수정 → 매장 메뉴 관리).
> 다만 자동 수집이 되는 게 원래 설계라 그대로 두면 안 될 것 같습니다.

#### 🆕 `kakao_place_id` 가 이 항목을 풀 수도 있습니다 — 갈라 봐 주세요

store 21 은 **`kakao_place_id` 없이 등록된 가게**입니다. 그때는 서버가 그 값을
안 내려줬고, 저희도 안 실어 보내고 있었습니다(§0-1 의 정정). 그래서 지금 `PENDING`
인 것이 **수집기가 고장난 것인지, 트리거 입력이 없어서 안 돈 것인지 구분이 안 됩니다.**

- **Q1-1a.** 대표메뉴 자동 수집은 **`kakao_place_id` 가 있어야만** 도나요?
  그렇다면 store 21 이 안 채워지는 건 정상이고, 저희가 고친 앱으로 KAKAO 후보를
  등록하면 해결됩니다.
- **Q1-1b.** `kakao_place_id` 없이도 돌아야 하는 설계라면(네이버로만 등록한 사장님이
  있습니다) 그때는 무엇으로 수집하나요?
- **Q1-1c.** store 21 처럼 **이미 만들어진 가게**에 나중에 `kakao_place_id` 를 붙여
  수집을 다시 걸 방법이 있나요? (`PATCH /stores/21` 로는 저장되지 않는 값입니다)

#### 실제로 태워 봤습니다 — **`kakao_place_id` 를 실어 보내도 안 돕니다**

고친 앱이 보내는 것과 **같은 Body** 로 KAKAO 후보를 새로 등록했습니다.

```
POST /stores  { name: "농민백암순대 본점", info_source: "KAKAO",
                external_channel_url: "http://place.map.kakao.com/17163273",
                kakao_place_id: "17163273", ... }          → 201  store 32
```

카카오맵에 메뉴가 실제로 올라와 있는 가게로 골랐습니다. **33분 동안 15초~5분 간격으로
재봤고 한 번도 변하지 않았습니다.**

```
[  0초] 기본정보 SUCCESS / 메뉴 PENDING / 사진 PENDING / 상권분석 PENDING · 메뉴 0
[  3분] 같음   (15초 간격 12회)
[ 30분] 같음   (5분 간격 6회)

같은 시각 store 21 (kakao_place_id 없이 등록) — 완전히 같은 상태
```

**그래서 §1-2 는 트리거 입력 문제가 아닙니다.** `kakao_place_id` 가 있는 가게와
없는 가게가 똑같이 `PENDING` 입니다. Q1-1a~c 는 그대로 유효하지만, **수집기 자체가
돌지 않는 쪽**을 먼저 봐 주시는 게 빠를 것 같습니다.

> ⚠️ store 32 는 이 확인 때문에 만든 것이라 **지울 방법이 없습니다** —
> `DELETE /stores/{id}` 가 API 에 없습니다. QA 계정에 가게가 하나 늘어난 채로
> 남습니다. 정리해 주실 수 있으면 지워 주세요.

### 1-2. ✅ 5.1 포맷 — **5건으로 늘었습니다** (2026-08-28 확인)

```
2026-08-26 오전    20건
2026-08-26 오후 ~ 08-28 오전   3건
2026-08-28 (지금)  5건  ← 늘었습니다
```

```
50  밈      주술회전 트랜지션
48  정보형  카페 추천 리뷰 릴스
49  챌린지  오츠카레 썸머 챌린지
55  밈      동그리오(챌린지)      ← 신규
56  정보형  동그리오(매장 홍보)   ← 신규
```

**앱은 수정 없이 그대로 뜹니다.** 새 두 건을 기존 세 건과 필드 단위로 대조해
빠진 값이 없는 것을 확인했고(`format_title`·`expected_duration_sec`·`reference_url`·
`guide_video_url`·`source_platform`·`requires_face` 전부 채워져 있습니다),
55번으로 직접 기획을 돌려 컷 6개와 구간까지 정상으로 받았습니다(프로젝트 214).

> **한 가지만 알려 드립니다 — 55번은 제목이 "동그리오(챌린지)" 인데 `format_type` 이
> `밈` 입니다.** 내용을 보니 춤이 아니라 반죽·굽기·토핑 같은 제조 과정이라 **`밈` 이
> 맞습니다.** 저희가 안무 화면을 제목이 아니라 `format_type` 으로 가르고 있어서
> 정확히 걸러졌습니다(§2-2). 앞으로도 제목의 "챌린지" 와 `format_type` 은 따로
> 봐 주시면 됩니다.

20건까지 돌아올 예정이라면 언제쯤인지만 알려주세요. 5건이 당분간의 정상이면
그대로 두셔도 앱은 문제없습니다.

---

## 2. 🟡 확인 부탁드리는 것

### 2-1. ✅ 참고 영상 구간 `start_ms` · `end_ms` — **받았습니다. 붙였습니다**

**2026-08-28 실측으로 값이 들어온 것을 확인했습니다.** 채워 주셔서 감사합니다.
Q2-1a·b·c 는 아래 실측으로 전부 답이 나와 **닫습니다.**

챌린지 포맷(49)으로 기획한 프로젝트 181 의 컷 7개입니다.

```
GET /tasks/{id}/guide  →  reference_video
  컷 1      0 ~  1000 ms      컷 5   8000 ~  9000 ms
  컷 2   1000 ~  4000 ms      컷 6   9000 ~ 11000 ms
  컷 3   4000 ~  5000 ms      컷 7  11000 ~ 12000 ms
  컷 4   5000 ~  8000 ms
```

한 영상(`e-dU9yQfmik`)을 **빈틈없이 쪼개서** 주시고, 구간 길이가 7.2
`target_duration_sec`(1·3·1·3·1·2·1초)과 정확히 일치합니다. 저희가 바라던 그대로입니다.

- **Q2-1b 답** — 기준은 `reference_video.reference_url` 이 맞았습니다. 앱도 이 값을
  먼저 쓰고, 없을 때만 포맷의 가이드 영상으로 떨어집니다.
- **Q2-1c 답** — `start_ms` 만 오는 경우는 없었습니다. 둘 다 오거나 둘 다 `null` 입니다.
  `target_duration_sec` 으로 끝을 계산하는 대비책은 넣지 않았습니다.

#### 앱 쪽 상태

촬영 화면 작은 창이 **지금 찍는 컷의 구간만 되풀이합니다.** 컷을 넘기면 같은 영상의
다른 구간으로 갈아탑니다 — 주소가 그대로라 영상을 다시 받지 않고 재생 위치만 옮깁니다.

> **한 가지 알려 드립니다 — 1초짜리 구간이 절반이 넘습니다.**
> 유튜브 임베드는 재생 위치를 ~0.25초 간격으로만 알려줘서, 그 보고를 보고 되감으면
> 끝점을 평균 0.12초·최악 0.25초 지나칩니다. **1초 구간에서는 25%** 라 눈에 띄게
> 덜컥거립니다. 그래서 앱은 위치를 보고 되감는 대신 **남은 시간만큼 타이머를 미리
> 걸어** 두는 방식으로 바꿨습니다(초과 0.016초). **서버에서 하실 일은 없습니다** —
> 다만 앞으로 구간을 나누실 때 1초보다 더 잘게 쪼개시면 저희 쪽 여유가 없다는 것만
> 참고 부탁드립니다.

#### ⚠️ 남은 것 하나 — 정보형에는 아직 `null` 입니다

정보형 포맷(48)으로 기획한 프로젝트 196 은 컷 4개 전부 `start_ms`·`end_ms` 가
`null` 입니다. 안무처럼 "이 동작을 따라 하세요" 가 아니라서 구간이 필요 없는 것이라면
**그대로 두셔도 됩니다** — 앱은 값이 없으면 반복을 끄고 영상 전체를 재생합니다.
의도한 것인지만 알려주세요.

### 2-2. 🆕 `guide_type: DANCE` 가 **한 번도 안 옵니다**

앱에는 안무 전용 촬영 화면이 있습니다. 참고 영상을 보면서 따라 추는 자리라 일반
촬영과 화면이 다릅니다. 그런데 거기로 가는 조건이 `guide_type === 'DANCE'` 인데
**그 값이 한 번도 오지 않습니다.**

**챌린지 포맷으로 직접 태워 봤습니다.**

```
POST /shorts-projects            video_format_id 4 "오츠카레 썸머 챌린지" (format_type 챌린지)
POST /shorts-projects/58/plan    200
GET  /shorts-projects/58/tasks   5개 — 전부 task_type "영상촬영"
       125 오버헤드 V 포즈 훅
       126 와이드 안무 A → 오버헤드 포인트
       127 와이드 안무 B → 오버헤드 포인트
       128 와이드 안무 C → 마지막 오버헤드 전환
       129 오버헤드 근접 마지막 포즈

GET /tasks/125|126|127/guide → 전부 guide_type "OVERLAY"
```

**2026-08-27 오전까지 재확인 — 여전히 전부 `OVERLAY` 입니다.** 다만 `reference_video` 는
이제 채워져서 옵니다(§2-2). 참고 영상이 있는데도 `guide_type` 만 안 바뀌는 상태입니다.

**컷 제목에 "안무" 라고 적혀 있는데도 `OVERLAY` 입니다.** 그래서 안무를 일반 촬영
화면에서 찍게 되고, 전용 화면은 코드만 남아 있습니다.

- **Q2-2a.** `DANCE` 를 실제로 내려 줄 계획인가요, 폐기됐나요? 폐기라면 앱에서 그
  화면과 분기를 걷어내겠습니다 — 지금처럼 두면 "언젠가 되는 기능" 으로 착각한 채 남습니다.
- **Q2-2b.** ~~DANCE 일 때 `reference_video` 는 채워지나요?~~ **닫혔습니다** —
  오늘 밤부터 `OVERLAY` 에서도 채워져 옵니다. 남은 건 `guide_type` 값 하나입니다.
- **Q2-2c.** **안무 순서**를 줄 계획이 있나요? 시안에는 촬영 전에 순서를 익히는
  화면이 있는데(`1-8박 인트로 포인트`, `9-16박 메인 안무` …) 그건 시안 목업이고
  서버에 대응하는 값이 없습니다(명세 전문에 `choreo` 0건). **채울 값이 없어서 그
  화면은 만들지 않았습니다.** 계획이 있다면 어떤 모양인지 알려주세요.

### 2-3. 3.6 로고 업로드 — 캐시 두 가지 (아직 못 재봤습니다)

- **Q2-3a.** 업로드 성공 뒤 `GET /stores/{id}` 를 다시 부르면 **즉시** 갱신된 URL 이
  오나요? CDN 캐시로 잠깐 예전 이미지가 올 수 있나요?
  (지연된다면 프론트가 응답의 `logo_url` 을 낙관적으로 먼저 보여주겠습니다)
- **Q2-3b.** 파일명이 매번 바뀌나요, 같은 URL 에 덮어쓰기인가요? 같은 URL 이면 앱이
  예전 이미지를 캐시합니다. (그 경우 `updated_at` 을 쿼리로 붙이겠습니다)

> **오늘 관찰한 것 (답의 절반):** `GET /stores/21` 의 `logo_url` 은 **presigned S3
> 주소**이고 파일명이 해시입니다 (`…/stores/21/logo/c7dca92e…jpg?X-Amz-Signature=…`).
> 업로드마다 해시가 새로 생기는 구조라면 **캐시 문제가 아예 없습니다.** 다만 그걸
> 확정하려면 같은 가게에 로고를 **두 번 올려 파일명이 바뀌는지** 봐야 하는데, 그러면
> 사장님 로고가 시험용으로 덮이므로 저희가 임의로 하지 않았습니다. **한 줄만
> 알려주시면 끝납니다.**

---

### 2-4. 🟡 **3.1 PATCH 가 주소·좌표·출처도 받아 주나요?** (2026-08-28 신규)

시안 8차가 매장 정보 수정 화면의 "매장 이름" 을 **입력칸에서 재검색 카드로** 바꿨습니다.
사장님이 "변경" 을 눌러 2.1 로 가게를 다시 찾고, 고른 후보로 매장 정보를 갱신하는 흐름입니다.

지금 앱이 3.1 로 보내는 값은 **이름·업종·전화 셋뿐**입니다. 매장을 바꾸면 아래도 함께
보내야 하는데 **한 번도 보내 본 적이 없어 받아 주시는지 모릅니다.**

```
PATCH /stores/{store_id}
{
  "name": "...", "category": "...", "phone": "...",
  "address": "서울 동작구 보라매로 87",     ← 받아 주시나요?
  "latitude": 37.49, "longitude": 126.92,   ← 받아 주시나요?
  "info_source": "KAKAO"                    ← 받아 주시나요?
}
```

받아 주시면 그대로 붙이겠습니다. 안 되면 3.1 에 이 필드들을 열어 주셔야 합니다.
**2.1 검색은 이미 필요한 값을 다 주고 있어**(이름·도로명·지번·전화·좌표·출처) 그쪽은 손볼 게 없습니다.

---

### 2-5. 🟡 **매장을 바꾸면 2.3 가져오기를 다시 돌릴 방법이 없습니다** (2026-08-28 신규)

위 2-4 와 한 묶음입니다. 매장을 다른 가게로 바꾸면 **메뉴·매장 사진·상권분석이 옛 가게 것으로
남습니다.** 그 값들은 2.3 가져오기가 채우는데, 지금 API 에는 **다시 돌리는 경로가 없습니다.**

```
GET  /stores/{id}/import-status   조회만 있습니다 (2.3)
POST /stores                       등록할 때 서버가 알아서 시작합니다 (2.2)
→ 이미 등록된 가게의 정보를 갈아끼울 방법이 없습니다
```

**요청:** 매장 정보가 바뀌었을 때 가져오기를 다시 시작하는 경로
(예: `POST /stores/{id}/import` 또는 3.1 PATCH 가 주소를 바꾸면 서버가 알아서 재시작).

⚠️ **이 항목은 §1-1 이 풀린 뒤라야 의미가 있습니다.** 지금은 2.3 자체가 `PENDING` 에서
안 넘어가서, 재시작 경로가 생겨도 결과가 비어 있습니다.

**그래서 앱은 이 화면을 아직 만들지 않았습니다.** 지금 만들면 사장님이 매장을 바꿨는데
메뉴는 옛 가게 것이 뜨는 꼴이 됩니다 — 두 항목의 답을 받고 착수하겠습니다.

---

### 2-6. 🟡 **16.1 `sns_account_name` 에 계정 이름이 아니라 내부 id 가 옵니다** (2026-08-28 신규)

마이페이지 ③ 줄에 연동한 인스타·유튜브 계정을 보여 줍니다. 그런데 사장님 화면에
**계정 이름 대신 긴 숫자가 뜹니다.**

```
GET /sns-connections
{ "connections": [
  { "id": …, "sns_platform": "INSTAGRAM",
    "sns_account_name": "17841…",     ← 사람이 읽는 이름이 아닙니다
    "token_expires_at": … } ] }
```

이름이 `account_name` 이라 저희는 사람이 읽는 이름으로 알고 그대로 찍고 있었습니다.

#### 🔴 2026-08-28 추가 — **유튜브는 이름이 아예 비어서 옵니다**

사장님이 유튜브 연동을 마치셨습니다. 연동 자체는 저장됐는데 `sns_account_name`
**이 비어 있습니다.** 그래서 매장정보 수정 화면에 계정 이름 자리가 비고, 앱은
**"계정 이름 없음"** 이라고 적을 수밖에 없습니다.

```
INSTAGRAM   sns_account_name = "17841…"   ← 내부 id (사람이 못 읽음)
YOUTUBE     sns_account_name = (빈 값)     ← 아예 없음
```

**연동은 성공했는데 화면에는 이름이 없습니다.** 어느 채널을 연결한 건지 사장님이
확인할 방법이 없습니다.

> 저희 쪽 화면 버그도 하나 있었고 고쳤습니다 — 이름이 비면 마이페이지가 연동
> 자체를 안 된 것으로 봐서 "연동하기" 로 떴습니다. 지금은 **목록에 있느냐로만**
> 판단합니다. 다만 **이름이 비는 것 자체는 서버에서 채워 주셔야 합니다.**

#### Q2-6a. `sns_account_name` 에 **사람이 읽는 계정 이름**을 넣어 주세요

- 인스타그램 → `@` 를 뺀 **username** (예: `dongguri_bakery`)
- 유튜브 → **채널명** 또는 핸들 (예: `동그리오` / `@dongguri`)

토큰을 들고 계신 쪽이 서버라 **서버만 이 값을 가져올 수 있습니다.**
연동을 끝낼 때 한 번만 부르면 되고, 그때 받은 값을 저장해 두시면 됩니다.

```
인스타  GET /me?fields=username           (Graph API)
유튜브  GET /youtube/v3/channels?part=snippet&mine=true
```

#### Q2-6b. **프로필 주소**를 함께 주실 수 있나요 — `profile_url` (선택)

사장님이 그 줄을 누르면 **자기 계정 페이지로 넘어가게** 하려고 합니다. 지금은
보낼 주소가 없습니다 — 명세 전체에서 계정을 가리키는 필드가 `sns_account_name`
하나뿐이고, 핸들도 주소도 없습니다.

Q2-6a 만 해결돼도 저희가 주소를 만들 수는 있습니다.

```
인스타  https://www.instagram.com/{username}
유튜브  https://www.youtube.com/@{handle}
```

다만 **유튜브는 채널명과 핸들이 다를 수 있어**(채널명 "동그리오" · 핸들 `@dongguri0`)
채널명만 오면 주소를 만들 수 없습니다. 그래서 `profile_url` 을 함께 주시는 쪽이
확실합니다. 여의치 않으면 **유튜브만이라도 핸들(`@…`) 또는 채널 id(`UC…`)** 를
주세요 — 채널 id 면 `youtube.com/channel/{id}` 로 만들 수 있습니다.

#### 앱은 그동안 이렇게 둡니다

값이 사람이 읽는 이름인지 id 인지 **모양으로 가려서**, id 로 보이면 숫자를 그대로
찍지 않고 "연동됨" 으로 둡니다. 눌러서 갈 곳도 만들 수 있을 때만 누르게 합니다.
Q2-6a 가 들어오면 **앱 수정 없이** 이름이 그대로 뜹니다.

---

### 2-7. 🔴 **유튜브는 동의까지 끝나는데 16.1 목록에 안 들어옵니다** (2026-08-28 신규)

사장님이 유튜브 연동을 **여러 번**, **끊고 다시**까지 하셨습니다. 구글 동의 화면을
**끝까지 통과**하는데도 마이페이지·매장정보 수정 두 화면 모두 계속 **"연동하기"**
입니다. 인스타그램은 같은 자리에서 바로 됩니다.

동의가 끝난다면 콜백까지는 도달합니다. **토큰 교환 이후 저장 단계**를 봐 주세요.

#### Q2-7a. 🔴 **유튜브 연동을 `INSTAGRAM` 으로 저장하고 있지는 않은지 봐 주세요**

지금 증상이 이것으로 **정확히** 설명됩니다. 유튜브 건이 `sns_platform='INSTAGRAM'`
으로 저장되면

- 앱은 `INSTAGRAM` 목록에서 **첫 번째**만 보여 주므로 인스타는 멀쩡해 보입니다
- `YOUTUBE` 는 한 건도 없으니 **"연동하기"** 로 남습니다
- 끊고 다시 해도 같은 자리에 또 쌓이니 **몇 번을 해도 그대로**입니다

**플랫폼은 `state` 안에 정확히 실려 있습니다.** 저희가 발급 결과를 열어 확인했습니다.

```
authorize?platform=INSTAGRAM  →  state.platform = INSTAGRAM   sub=25
authorize?platform=YOUTUBE    →  state.platform = YOUTUBE     sub=25
```

콜백에서 **플랫폼을 `state` 에서 읽고 있는지**, 혹시 기본값이나 다른 경로로
`INSTAGRAM` 을 쓰고 있지는 않은지 확인 부탁드립니다.

> **DB 한 줄로 바로 가려집니다** — 그 사용자(`sub=25`)의 `sns_connections` 를 열어
> `INSTAGRAM` 이 **두 줄**이면 이 건이 맞습니다.

#### Q2-7b. 그게 아니면 — 저장이 실패하고 있습니다

짚이는 것 둘입니다.

1. **그 구글 계정에 유튜브 채널이 없을 수 있습니다.** 채널을 한 번도 만들지 않은
   계정이 흔합니다. `channels?mine=true` 가 비면 `sns_account_name` 을 못 채워
   저장이 실패했을 수 있습니다. **이 경우를 따로 구분해 주시면** 사장님께
   "채널을 먼저 만들어 주세요" 라고 안내할 수 있습니다.
2. **`state` 가 10분 만료입니다.** 인스타·유튜브 둘 다 600초인데 구글은 계정 선택
   → 로그인 → 2단계 인증 → 동의로 단계가 더 많습니다. **유튜브만 20~30분으로
   늘려 주실 수 있나요?**

#### 앱 쪽은 아닙니다 — 확인한 것

- 두 화면 모두 **같은 `GET /sns-connections` 하나**를 보고 판정도 똑같이
  `sns_platform === 'YOUTUBE'` 입니다. 한쪽만 틀릴 구조가 아닙니다
- `SnsPlatform` enum 이 서버·앱 모두 `INSTAGRAM` · `YOUTUBE` 로 일치합니다
- **`authorize?platform=YOUTUBE` 는 정상입니다**(200) — `redirect_uri`
  `https://sarils.p-e.kr/sns-connections/callback` · `access_type=offline` ·
  `prompt=consent` · 스코프 둘 다 정상
- 앱은 응답을 플랫폼으로 거르기만 하고 가공하지 않습니다

#### Q2-7c. 실패 이유를 앱이 알 방법이 필요합니다 (옛 Q6-a 와 같은 건)

지금 앱은 **연동 목록의 개수가 늘었는지**로만 성공을 판단합니다. 늘지 않으면
"아직 연동되지 않았어요" 라고만 말할 수 있습니다 — 창을 닫으신 건지, 채널이
없는 건지, 저장이 실패한 건지 구분할 수가 없습니다. **이번 건도 사장님이 원인을
모른 채 몇 번을 반복하셨습니다.** 콜백 실패 사유를 어디든 남겨 주시면 무엇을
하면 되는지 말할 수 있습니다.

#### 출시 전 별건 — 구글 검수

`yt-analytics.readonly` · `youtube.readonly` 는 구글이 **민감**으로 분류합니다.
OAuth 동의 화면이 아직 **"테스트"** 상태라면 테스트 사용자로 등록된 계정만 됩니다.
지금은 사장님 계정이 통과하니 등록돼 있는 것으로 보이지만, **출시 전에는
프로덕션 게시 + 구글 검수가 필요하고 심사에 시간이 걸립니다.** 일정에 미리
잡아 주세요.


## 3. ⚪ 편집 완료 푸시 — **계약 확정. 남은 건 payload 키 이름 하나입니다**

Q3-a·b·c 에 답 주셔서 **토큰 쪽은 끝났습니다.** 말씀만 믿지 않고 실서버로
세 방향을 직접 쏴 봤습니다 — 셋 다 회신하신 그대로였습니다.

```
POST /users/me/push-tokens
  { "push_token": "ExponentPushToken[...]", "platform": "ANDROID" }  → 200
  { "token":      "ExponentPushToken[...]", "platform": "ANDROID" }  → 422
       VALIDATION_ERROR  "push_token: Field required"
  같은 push_token 다시                                               → 200  (upsert 확인)
```

**저희가 맞추는 것 셋**

| | |
|---|---|
| 필드명 | `push_token` 으로 보냅니다 (저희 제안 `token` 은 폐기) |
| 전송 시점 | upsert 라 하셨으니 **앱 실행할 때마다 그냥 보냅니다.** 마지막 값을 기억하는 장치를 안 만들어도 되어 오히려 간단해졌습니다 |
| 삭제 | 만들지 않습니다. 탈퇴가 소프트 삭제이고 발송 시점에 활성 사용자로 거른다는 설명에 동의합니다 |

**분담과 딥링크는 지난번 그대로입니다.** Expo 발송 서비스를 쓰므로 BE 는 Firebase
자격증명을 만질 일이 없고, 키는 EAS(프론트 계정)에만 등록합니다. **Firebase 프로젝트는
프론트가 만듭니다.** 딥링크는 **`reals://project/{shorts_project_id}/result`**,
앱 scheme 은 `reals` 입니다.

### 남은 질문 하나

- **Q3-a. 알림 payload 에 넣어 주실 값**은 `shorts_project_id` 하나면 충분합니다.
  키 이름만 확정해 주세요 — `data: { "shorts_project_id": 56 }` 로 받겠습니다.
  `video_output_id` 는 그 화면이 15.1 로 다시 조회하므로 필요 없습니다.

> **14.1 중복 호출도 함께 고쳐 주신다니 감사합니다.** 앱 쪽은 이미 막아 뒀습니다 —
> `PENDING`·`PROCESSING`·`COMPLETED` 인 프로젝트에는 14.1 을 다시 걸지 않고 붙기만
> 합니다. 서버가 멱등하게 동작하면 다른 경로(재시도·푸시 복귀)에서 겹쳐 들어가도
> 안전해집니다.

### ✅ 앱 쪽은 붙였습니다 (2026-08-27)

Firebase 프로젝트를 만들고 FCM 키를 EAS 에 등록했습니다. 앱에는
`expo-notifications` 를 넣고 **권한 요청 → 토큰 발급 → 저장 API 전송 → 알림을 눌렀을
때 이동**까지 연결했습니다(`src/lib/push.ts`).

```
앱이 보내는 것   POST /users/me/push-tokens
                { "push_token": "ExponentPushToken[...]", "platform": "ANDROID" }
보내는 시점      로그인 상태가 될 때마다 — upsert 라 기억하지 않고 매번 보냅니다
누르면 가는 곳   내보내기 화면 (프로젝트 결과)
```

네이티브라 APK 를 새로 만들어야 해서 **오늘 빌드(1.0.1)에 넣었습니다.**
**BE 쪽은 기다리실 것이 없습니다** — 저장 API 는 이미 동작합니다
(위 200 이 실제로 저장된 것입니다. 시험용으로 넣은 `ExponentPushToken[QA-VERIFY-0826]`
은 실제 기기 토큰이 아니니, 발송 시험 때 걸리면 무시하시거나 지우셔도 됩니다).

**발송 시험을 하실 준비가 되면 알려주세요.** 실기기에서 토큰을 받아 사장님 계정으로
등록해 두고, 그 토큰으로 한 번 쏴 보시면 전 구간이 확인됩니다.

---

## 4. ⚪ 범위 확인 (이전 질문 유지)

### 4-1. 유료 플랜 — MVP 인가요?

시안에 요금제가 구체적으로 설계돼 있습니다(Free 월 3개 / Pro ₩9,900 무제한 ·
워터마크 제거 · 인사이트). **지금은 화면만 만듭니다** — "Pro로 업그레이드" 는 비활성 +
"준비 중입니다" 입니다.

**Q4-1.** MVP 범위라면 아래가 필요합니다.
- 현재 플랜 + 이번 달 남은 횟수 조회
- 3개 소진 시 4.1 생성 차단 (에러 코드는?)
- 워터마크 분기 (15.1 출력)
- 인사이트 접근 제어 (3.5 를 Free 는 못 보게)
- 결제 연동

### 4-2. 기존 대기 항목

| 항목 | 질문 | 영향 |
|---|---|---|
| 16.1 연동 | SNS 연동 **없이도** 17.x 성과 수집이 되나요? | 작업량 최대 분기점. 안 되면 성과 화면 전체가 연동 뒤로 밀립니다 |
| R11 직접 녹음 | MVP 범위인가요? | 현재 화면 없음 |
| 4.2 `success_metric` | 자유 입력인가요 선택지인가요? | 선택지면 목록 필요 |
| 5.1/5.2 썸네일 | YouTube 가 아닌 포맷의 썸네일은 어떻게 얻나요? | 지금은 YouTube 만 나옵니다 |
| 비밀번호 재설정 | Phase 2 로 올려주실 수 있나요? | 지금은 이메일 문의 안내로 대체 |
| 아이디 로그인 | 회원가입 첫 칸이 시안대로 "아이디" 인데 로그인은 이메일입니다. 아이디 컬럼을 만드실 건가요? | 지금은 아이디를 `name` 으로 보내고 있어 마이페이지에 **이름 대신 아이디**가 표시됩니다 |
| 홈 릴스 채널 프로필 | 유튜브 채널 썸네일(`channel_thumbnail_url`)을 주실 수 있나요? | 지금은 빈 동그라미입니다. YouTube Data API 키 없이는 프론트가 못 구합니다 |

---

## 5. ⚪ 저희가 아직 못 재본 것

| 항목 | 왜 못 했나 |
|---|---|
| **15.1 · 15.2 실물** | 렌더가 끝난 프로젝트가 있어야 하는데, 그러려면 촬영본을 올려야 합니다. 스키마는 확인했고(§0-1 의 15.1 항) **실제 값이 어떻게 채워져 오는지**를 못 봤습니다 |
| **인스타·구글 실제 로그인** | 저희에게 그 계정이 없습니다. **`client_id` 가 고쳐져 로그인 화면까지 정상으로 열리는 것은 확인했고**(§0-2), 그 뒤 로그인→콜백→토큰 교환은 사장님 계정으로 한 번 눌러 봐야 합니다 |
| **3.6 로고 캐시** | §2-3 |
| **푸시 전 과정** | Firebase·APK 재빌드가 선행입니다 (§3). **토큰 저장 API 는 오늘 실제로 200 을 받아 확인했습니다** — 못 재본 것은 발송·수신·딥링크입니다 |

> 참고로 **대화 → 추천 → 채택 → 촬영 준비까지는 오늘 실제로 태웠습니다.**
>
> ```
> POST /stores/21/shortform-sessions                 201
> POST /shortform-sessions/{id}/turns × 8             200  (선택지 → 확인 → 추천 3건)
> POST /shortform-sessions/{id}/accept                201  (project 56)
> GET  /shorts-projects/56/tasks · /video-formats/46 · /tasks/119/guide   200
> POST /shortform-sessions/{id}/recommendations/next  200  (다른 추천도 정상)
> ```

---

## 부록. 프론트가 지키고 있는 것

- 없는 값은 `—`. **0 으로 쓰지 않습니다** (0 은 "실제로 0" 이라는 주장이라서)
- `AI추론` 출처는 "AI 추측" 배지로 실제 데이터와 구분
- 저장(POST/PATCH/DELETE)은 상태를 **실제로 바꾸고**, 재조회 반영까지 확인합니다.
  오늘 "저장 실패인데 성공했다고 말하던" 곳을 하나 찾아 고쳤습니다
- 화면 코드에 URL·snake_case·hex 색상을 쓰지 않습니다
- **시안에 그려져 있어도 서버가 안 주는 값은 비웁니다.** 지어내면 사장님이 그 값을
  믿고 촬영 계획을 세웁니다 — §2-2 의 안무 순서가 그래서 비어 있습니다.
  **비워 둔 자리는 값이 오면 바로 채웁니다** — 지번(§0-1)과 추천 카드의 해시태그·영상
  (§0-2)이 그렇게 채워졌습니다. 포기한 게 아니라 자리를 잡아 둔 것입니다

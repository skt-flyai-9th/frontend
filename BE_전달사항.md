# BE 전달사항

**최신화: 2026-08-26 (저녁 재확인).** 이 문서의 상태코드·응답 키는 전부 **실서버
(`https://sarils.p-e.kr`)를 직접 호출한 실제 응답**입니다. 추측으로 적은 줄은 없습니다.

이번 재확인에서 **15.1 음원(§2-6)이 해결됐고**, 인스타 연동이 막히는 **정확한 원인을
찾았습니다(§2-8)**. 나머지 🔴 두 건은 오전과 같은 상태입니다.

해결된 항목은 §5 에 한 줄씩만 남기고 본문에서 뺐습니다.

```
확인에 쓴 계정   realsqa0826@test.co.kr   (user 25)
가게            스타벅스 한국프레스센터점 (store 21)
```

---

## 0. 한눈에

| | 항목 | 상태 |
|---|---|---|
| 🔴 | **16.1 인스타 `client_id` 가 페이스북 App ID** | **연동이 여기서 막힙니다.** 인스타 authorize 에는 Instagram App ID 를 넣어야 합니다 — 증거·고치는 법 §2-8 |
| 🔴 | 2.3 가져오기가 `PENDING` 에서 안 넘어감 → 메뉴 0개 | 저녁 재확인도 그대로. 홍보 대상을 고를 수 없습니다 |
| 🔴 | 5.1 포맷이 20건 → **3건** | 저녁 재확인도 3건. 홈 피드가 세 편뿐입니다 |
| 🟡 | 16.1 요청 범위가 **전부 읽기 전용** | 16.2 자동 게시가 불가능해 보입니다 (§2-8) |
| 🟡 | 추천 응답에 `video_format_id` 가 없습니다 | 카드의 태그·영상을 채울 수 없습니다 (§2-1) |
| 🟡 | 2.1 에 `kakao_place_id` 가 아예 없습니다 | 대표메뉴 자동 수집 트리거가 안 걸립니다 (§2-2) |
| 🟡 | **2.1 에 지번 주소가 없습니다** | 시안 매장 확인 시트의 한 줄을 못 채웁니다 (§2-9, 신규) |
| 🟡 | **참고 영상의 구간(start/end)이 없습니다** | 컷마다 해당 구간만 반복 재생하려는데 값이 없습니다 (§2-10, 신규) |
| 🟡 | 6.1·6.2·6.3 이 404 — 폐기 확인 + 명세 정리 | 저녁 재확인도 404 |
| 🟡 | 14.1 을 진행 중에 다시 부르면? | 앱은 이미 막아 뒀습니다. 서버 동작만 확인 부탁 (§2-4) |
| 🟡 | 3.1 명세 Body 가 실제 서버와 다릅니다 | 서버는 다 받습니다 — 문서만 (§2-5) |
| 🟡 | 3.6 로고 캐시 | 이전 질문 유지 (§2-7) |
| ✅ | **15.1 음원 메타데이터** | **해결됐습니다.** `publish_kit.track` 확인 — 화면에 붙였습니다 (§5) |
| ⚪ | 유료 플랜 MVP 여부 · 기존 대기 항목 | §4 |

---

## 1. 🔴 지금 막고 있는 것

### 1-1. 2.3 가져오기가 `PENDING` 에서 안 넘어갑니다 — 메뉴가 0개입니다

```
GET /stores/21/import-status
  overall_status: IN_PROGRESS
  기본정보 SUCCESS / 메뉴 PENDING / 사진 PENDING / 상권분석 PENDING
GET /stores/21/menus             → { "menus": [] }
GET /stores/21/target-customers  → { "target_customers": [] }
```

2.1 검색으로 등록한 **네이버 유명 프랜차이즈 지점**인데도 메뉴가 하나도 없습니다.
등록 직후부터 여러 시간이 지나도 그대로입니다.

**막히는 것:** "어떤 메뉴를 홍보할까요?" 에서 고를 것이 없습니다. AI 추천 대화에서도
메뉴를 물어볼 때 사장님이 직접 타이핑해야 합니다.

프론트는 메뉴가 0개면 "등록된 메뉴가 없습니다. 직접 적어 주세요" 로 빠질 길을 열어
두었지만, 자동 수집이 되는 게 원래 설계입니다.

### 1-2. 5.1 포맷이 20건 → **3건**으로 줄었습니다

```
2026-08-26 오전   GET /video-formats?sort=trending  → 20건
2026-08-26 오후   GET /video-formats               → 3건
                  GET /video-formats?sort=trending → 3건
                  GET /video-formats?sort=latest   → 3건
```

정렬을 바꿔도 3건입니다. 홈 피드가 카드 세 장으로 끝나고 관심 목록도 그 안에서만
고를 수 있습니다.

**의도한 것인가요?** 트렌드 클러스터를 새로 도는 중이라 잠깐 줄어든 것이라면
언제 복구되는지, 3건이 정상이라면 그 이유를 알려주세요.

---

## 2. 🟡 확인 부탁드리는 것

### 2-1. 추천 응답에 **포맷·썸네일·태그가 없습니다**

R06 추천이 주는 것은 이게 전부입니다.

```
recommendation_id · project_title · title · concept
editing_template_id · editing_template_version
```

시안(`image (1).png`, 6차 `RecoCard`)의 추천 카드는 이렇게 생겼습니다.

```
제목 (2줄)
AI 한줄요약
#촬영시간5분 #1인촬영 #얼굴미노출     ← 해시태그 3개
[ 숏츠 영상 임베드 126x224 ]          ← 영상
[ 바로 촬영하기 ]
```

**해시태그와 영상을 채울 값이 없습니다.** 포맷 번호(`video_format_id`)는 채택한
뒤에야 응답으로 돌아옵니다. 지어내지 않기 위해 그 두 자리를 비워 두었고,
지금 카드는 제목·요약·버튼만 있습니다.

**Q2-1.** 추천 단계에서 `video_format_id` 를 함께 주실 수 있나요?
그러면 나머지(썸네일·촬영시간·난이도·얼굴노출)는 프론트가 5.2 로 스스로 채웁니다.

### 2-2. 2.1 검색 응답에 `kakao_place_id` 가 **아예 없습니다**

```
GET /stores/search?keyword=스타벅스
응답 키: source, name, address, phone, latitude, longitude,
        category, distance_m, external_channel_url
```

명세 2.1 에는 있다고 되어 있어 프론트가 2.2 등록 때 그대로 돌려보내도록 만들어
뒀는데, **받을 값이 없어 항상 null 로 나갑니다.** 대표메뉴 자동 수집 트리거가
동작하지 않을 텐데(§1-1 과 관련 있어 보입니다), 명세에서 빼는 게 맞는지
서버에 추가하실 건지 알려주세요.

> 좌표는 정상입니다 (`latitude: 37.5672475`, `longitude: 126.9780493`).

### 2-3. 6.1 · 6.2 · 6.3 은 폐기된 것으로 이해했습니다 — **명세에서 지워 주세요**

```
GET  /shorts-projects/{id}/quiz-questions  → 404
POST /shorts-projects/{id}/quiz-answers    → 404
```

R06 이 대화형 에이전트(`/shortform-sessions/…`)로 바뀌면서 없어진 것으로 보입니다.
맞다면 명세에서 지워 주세요. 프론트는 이 경로를 "서버에 아직 없는 것" 으로 보고
앱 안에서 흉내 내는 장치를 두고 있었는데, 확답을 받으면 걷어내겠습니다.

### 2-4. 14.1 을 **진행 중에 다시 호출하면** 어떻게 되나요?

**앱 쪽은 고쳤습니다** — 이미 `PENDING`·`PROCESSING`·`COMPLETED` 인 프로젝트에는
14.1 을 다시 걸지 않고 붙기만 합니다(2026-08-26). 그래도 서버 동작은 알아야
합니다. 다른 경로(재시도·푸시 복귀)에서 겹쳐 들어갈 수 있습니다.

**Q2-4.** 이미 `PROCESSING` 인 프로젝트에 14.1 을 다시 부르면
① 새 렌더를 또 만드나요 ② 기존 것을 그대로 돌려주나요 ③ 에러인가요?

②라면 프론트는 지금처럼 두어도 안전하고, ①이면 프론트가 막아야 합니다.

### 2-5. 3.1 명세 Body 가 실제 서버와 다릅니다 (서버가 맞습니다)

명세 3.1 PATCH 의 Body 예시는 `business_hours` · `brand_tone` · `brand_color`
세 개뿐인데, **실제 서버는 더 받습니다.** 전부 200 이고 값도 저장됩니다.

```
PATCH /stores/21  { name, category }                          → 200, 저장됨
PATCH /stores/21  { business_hours, brand_tone, brand_color }  → 200, 저장됨
PATCH /stores/21  { address, phone }                           → 200, 저장됨
```

프론트는 문제없이 쓰고 있습니다. **명세 문서만 실제에 맞게 갱신해 주세요.**

### 2-6. 15.1 음원 — ✅ **해결됐습니다. 확인 질문 하나만 남습니다**

요청드린 필드가 그대로 들어왔습니다. 감사합니다.

```
PublishKit → caption · hashtags · post_note · track
TrackInfo  → mode · title · artist · start_sec · end_sec · mood
```

앱 내보내기 화면에 붙였습니다. 다만 **`mode` 에 따라 화면이 갈리게** 만들었는데,
저희가 읽은 뜻이 맞는지만 확인 부탁드립니다.

| mode | 앱이 그리는 것 | 저희가 이해한 뜻 |
|---|---|---|
| `FIXED` | "곡명 - 아티스트" + 사용 구간 | 이 곡을 쓰면 된다 |
| `SUGGESTED` | "어울리는 음원" + `mood` | 곡은 못 정했고 분위기만 준다 |

- **Q2-6.** 맞나요? 특히 `SUGGESTED` 일 때 `title` 이 `null` 로 오는 게 정상인가요?
  (그때 곡명 카드를 그리면 빈 카드가 됩니다. 지금은 분위기만 보여줍니다.)
  `start_sec`·`end_sec` 은 **원곡에서의 위치**로 이해했습니다 — 완성 영상 기준이 아니라요.

### 2-7. 3.6 로고 업로드 — 캐시 두 가지 (이전 질문 유지)

- **Q2-7a.** 업로드 성공 뒤 `GET /stores/{id}` 를 다시 부르면 **즉시** 갱신된 URL 이
  오나요? CDN 캐시로 잠깐 예전 이미지가 올 수 있나요?
  (지연된다면 프론트가 응답의 `logo_url` 을 낙관적으로 먼저 보여주겠습니다)
- **Q2-7b.** 파일명이 매번 바뀌나요, 같은 URL 에 덮어쓰기인가요?
  같은 URL 이면 앱이 예전 이미지를 캐시합니다. (그 경우 `updated_at` 을 쿼리로 붙이겠습니다)

### 2-8. 🔴 16.1 OAuth — **막히는 원인을 찾았습니다.** `client_id` 를 바꿔 주세요

**결론부터: 서버가 인스타 authorize 에 넣는 `client_id` 가 잘못됐습니다.**
아래 ① 이 그 증거이고, ②③ 은 같이 확인 부탁드리는 것들입니다.

앱과 서버의 연결 자체는 정상입니다 —

```
로그인(realsqa0826@test.co.kr)  → 200
GET /sns-connections            → 200 {"connections":[]}
GET .../authorize?platform=INSTAGRAM → 200, 실제 인스타 로그인 화면까지 열림
GET .../authorize?platform=YOUTUBE   → 200, 실제 구글 로그인 화면까지 열림
GET /sns-connections/callback (엉터리 code) → 400 "연동에 실패했습니다." 안내 페이지
```

앱에서 「Instagram 계정 연동 → 계속하기」를 누르면 authorize 를 부르고 브라우저가
열립니다. **여기까지는 됩니다.** 실제 계정 로그인은 저희가 가진 인스타·구글 계정이
없어 못 해봤습니다.

#### 🔴 새 질문 — 요청 범위가 화면 문구와 달랐습니다

`authorize_url` 의 `scope` 를 그대로 읽어 봤습니다.

| 플랫폼 | 서버가 실제로 요청하는 범위 |
|---|---|
| INSTAGRAM | `instagram_business_basic` · `instagram_business_manage_insights` |
| YOUTUBE | `yt-analytics.readonly` · `youtube.readonly` |

**전부 읽기 전용이고, 올리는 권한이 없습니다.** 그런데 앱 동의 화면은 시안 문구대로
"게시물 업로드 권한" 이라고 적고 있었습니다. 사장님께 거짓을 말하는 셈이라 **실제
범위대로 고쳤습니다**(프로필 정보 · 성과 읽기).

- **Q2-8e.** 이 범위가 맞나요? 그렇다면 **16.2 게시(자동 업로드)는 이 연동으로는
  불가능합니다.** 게시는 PENDING_LINK(사장님이 직접 올리고 링크 연결)만 남는 것으로
  이해하면 되나요? 아니면 게시용 scope 를 나중에 추가하실 계획인가요?
- **Q2-8f.** `instagram_business_*` 는 **비즈니스·크리에이터 계정 + 페이스북 페이지
  연결**이 있어야 통과합니다. 개인 계정은 로그인까지 되고 마지막에 막힙니다.
  그리고 Meta 앱이 개발 모드면 **테스터로 등록된 계정만** 됩니다.
  **"앱에서 연동이 안 된다" 는 보고는 이 둘 중 하나일 가능성이 큽니다.**
  Meta 앱·Google OAuth 동의 화면이 지금 **개발/테스트 모드인가요, 게시(공개) 상태인가요?**
  테스트 모드면 연동해 볼 계정을 테스터로 등록해 주셔야 합니다.
  앱에는 계정 조건 안내를 동의 화면에 넣어 뒀습니다.

#### 🔴 원인을 찾았습니다 — `client_id` 가 **페이스북 App ID** 입니다 (2026-08-26)

사장님 쪽 오류 원문입니다. **비즈니스 계정 + 페이스북 페이지 연결이 된 상태**였습니다.

```
유효하지 않은 요청: 요청 매개변수가 유효하지 않습니다:
Invalid scope: instagram_business_basic+instagram_business_manage_insights
```

처음에는 `instagram_business_manage_insights` 가 앱에 안 열린 것으로 봤는데,
scope 만 바꾼 주소 4개를 시험해 보니 **`instagram_business_basic` 하나만 넣은 것까지
전부 막혔습니다.** 특정 권한 문제가 아니라는 뜻이라 앱 설정 쪽을 팠습니다.

**결정적 증거 — 이 id 는 페이스북 앱 다이얼로그가 받습니다.**

```
GET facebook.com/v21.0/dialog/oauth?client_id=1234567890123456  (아무 숫자)
   → /oauth/error/?error_code=PLATFORM__INVALID_APP_ID
     "앱 ID 오류: 입력된 앱 ID가 유효한 앱 ID와 다릅니다."

GET facebook.com/v21.0/dialog/oauth?client_id=1541622377157897  (지금 쓰는 id)
   → 정상. 페이스북 로그인 화면이 뜹니다 (앱 ID 오류 없음)
```

**`1541622377157897` 은 유효한 페이스북 App ID 입니다.**

그런데 메타 문서는 `https://www.instagram.com/oauth/authorize` 의 `client_id` 가
**"Instagram App ID" — 페이스북 App ID 가 아니라고** 명시합니다.

> App Dashboard > Instagram > API setup with Instagram login >
> 3. Set up Instagram business login > **Business login settings > Instagram App ID**

이 둘은 **서로 다른 숫자**입니다. 페이스북 App ID 를 인스타 authorize 에 넣으면
인스타는 그 앱의 Instagram-Login 설정을 못 찾고, 등록된 scope 가 없으니
**무슨 scope 를 넣든 `Invalid scope`** 가 납니다. 관측된 증상과 정확히 맞습니다
(넷 다 실패 · 기본 권한 하나만 넣어도 실패).

- **Q2-8g. 아래 둘 중 하나로 고쳐 주세요.**

  **(가) 지금 흐름 유지 — `client_id` 만 교체 (권장)**
  App Dashboard > Instagram > API setup with Instagram login > Business login settings
  에서 **Instagram App ID** 를 찾아 그 값으로 바꾸시면 됩니다. 같은 화면의
  **OAuth redirect URI** 목록에 `https://sarils.p-e.kr/sns-connections/callback` 이
  들어 있는지도 같이 확인해 주세요. 앱 코드는 손댈 게 없습니다.

  **(나) 페이스북 로그인 방식으로 전환**
  authorize 주소를 `facebook.com/v21.0/dialog/oauth` 로 바꾸고 scope 를
  `instagram_basic` · `instagram_manage_insights` · `pages_show_list` 등으로 교체.
  지금 id 를 그대로 쓸 수 있지만, 사장님이 **페이스북 계정으로** 로그인하게 됩니다.
  40~60대 사용자에게는 (가)가 낫다고 봅니다.

  어느 쪽이든 **앱은 서버가 주는 `authorize_url` 을 열기만 하므로 프론트 수정은 없습니다.**

- **Q2-8h.** 유튜브도 같은 종류의 벽이 있습니다. `yt-analytics.readonly` ·
  `youtube.readonly` 는 구글 **민감 범위**라, OAuth 동의 화면이 미검증이면
  **테스트 사용자로 등록된 계정만** 통과합니다. 지금 게시 상태인가요, 테스트인가요?
  (인스타를 고친 뒤 유튜브도 같은 계정으로 한 번 확인이 필요합니다.)

#### 이전 질문 (그대로 유지)

붙이고 있는 흐름입니다.

```
① 앱 → GET /sns-connections/authorize?platform=INSTAGRAM
② 앱 → 받은 authorize_url 을 브라우저로 열기
③ (서버가 콜백 처리 — 앱 관여 없음)
④ 앱 → 포그라운드 복귀 감지 → GET /sns-connections 로 확인
```

- **Q2-8a.** ④의 복귀 감지는 앱 `AppState` 로만 판단합니다. **동의하지 않고 그냥
  브라우저를 닫고 돌아온 경우**에도 복귀로 잡힙니다. 그때 목록이 그대로면
  "연동 안 됨" 으로 처리하려는데 맞나요? "진행 중" 같은 중간 상태가 생기나요?
- **Q2-8b.** `authorize_url` 발급 자체가 실패할 수 있나요? 에러 코드를 알려주세요.
- **Q2-8c.** 연동 완료 페이지 문구는 "연결됐습니다. 앱으로 돌아가 주세요." 로 확정인가요?
- **Q2-8d.** 배포 도메인이 정해지면 알려주세요. 실제 연동 테스트의 선행 조건입니다.

### 2-9. 🆕 2.1 검색에 **지번 주소**를 넣어 주실 수 있나요?

시안 8차의 매장 확인 시트(`매장조회지도.jpg`)는 주소를 두 줄로 보여줍니다.

```
📍 서울 동작구 보라매로 87
   지번 신대방동 395-69      ← 이 줄
```

그런데 2.1 응답에 지번이 없어 **줄 자체를 안 그렸습니다**(지어내지 않기).

```
GET /stores/search?keyword=스타벅스
응답 키: source, name, address, phone, latitude, longitude,
        category, distance_m, external_channel_url
```

- **Q2-9.** `jibun_address` 를 추가해 주실 수 있나요?
  40~60대 사장님은 도로명보다 지번으로 자기 가게를 확인하는 경우가 많아,
  "내 가게가 맞나" 를 가르는 데 실제로 쓰이는 값입니다. 내려오면 앱은 그
  자리에 한 줄 추가만 하면 됩니다.

> 참고로 **`phone` 도 자주 `null`** 입니다(스타벅스 한국프레스센터점 실측).
> 앱은 값이 있을 때만 전화 줄을 그립니다. 네이버에 번호가 있는데 안 오는
> 것이라면 같이 봐 주세요.

### 2-10. 🆕 참고 영상의 **구간(start/end)** 을 태스크마다 주실 수 있나요?

사장님 요청입니다 — 촬영 화면의 유튜브 참고 영상을 **그 컷에 해당하는 구간만
반복 재생**하고 싶습니다. 지금은 영상 전체가 처음부터 재생돼서, 사장님이 지금
찍을 동작이 몇 초에 나오는지 직접 찾아야 합니다.

**서버가 이미 그 개념을 갖고 있는 것으로 보입니다.**

```
GET /shorts-projects/56/scenes
  scenes[0].shot_type          = "가이드 구간 재현"   ← 구간을 재현하라고 적혀 있음
  scenes[0].target_duration_sec = 3                   ← 길이는 있음
GET /tasks/119/guide
  broll_shot.shot_type          = "가이드 구간 재현"
```

**그런데 그 구간이 몇 초부터인지는 어디에도 없습니다.** 명세 전체에서 `start_sec`·
`end_sec` 을 가진 스키마는 `TrackInfo`(음원) 하나뿐이었습니다.

- **Q2-10a.** 9.1 `reference_video` 또는 7.2 `scenes[]` 에 아래를 넣어 주실 수 있나요?

  ```
  reference_segment: { start_sec: 4.5, end_sec: 7.5 }
  ```

  프론트는 이 값만 있으면 임베드 플레이어를 그 구간에서 반복시킬 수 있습니다.
  (유튜브 IFrame API 의 postMessage 채널을 쓰므로 별도 라이브러리·빌드가 필요 없습니다.)

- **Q2-10b.** 지금 `reference_video` 가 **OVERLAY 타입 태스크에서는 `null`** 로 옵니다.
  스키마 설명에 "댄스·안무 가이드용" 이라고 되어 있어 의도된 것 같은데, 그러면
  일반 촬영 컷에서 앱이 트는 참고 영상은 **포맷의 대표/가이드 영상**입니다.
  구간 정보를 준다면 그 영상 기준으로 이해하면 될까요?

  ```
  GET /tasks/119/guide → {"guide_type":"OVERLAY", "reference_video": null, ...}
  ```

- **Q2-10c.** `target_duration_sec`(3초)이 곧 구간 길이인가요? 그렇다면 `start_sec`
  하나만 주셔도 됩니다 — 끝은 `start_sec + target_duration_sec` 으로 계산하겠습니다.

> 참고: 예전에 이 기능을 넣었다가 뺐습니다(`20edec3`). 그때는 문서 안의 `<video>`
> 요소를 직접 잡아 조종했는데, 유튜브가 내부적으로 그 노드를 갈아끼워서 자주
> 먹통이 됐습니다. 지금은 IFrame API 로 바꿔서 같은 문제가 생기지 않습니다 —
> **막는 것은 구간 값이 없다는 것 하나뿐입니다.**

---

## 3. ⚪ 아직 못 재본 것

**15.1 · 15.2 실물은 아직 확인하지 못했습니다.** 렌더가 끝난 프로젝트가 있어야
하는데, 그러려면 촬영본을 올려야 합니다(외부에서 영상 업로드까지는 못 했습니다).

15.1 의 **스키마**는 확인했습니다(§2-6 track). 실제 값이 어떻게 채워져 오는지를
아직 못 본 것입니다.

**대화 → 추천 → 채택 → 촬영 준비까지는 오늘 실제로 태웠습니다.**

```
POST /stores/21/shortform-sessions          → 201
POST /shortform-sessions/{id}/turns × 8      → 200 (선택지 → 확인 → 추천 3건)
POST /shortform-sessions/{id}/accept         → 201 (project 56)
GET  /shorts-projects/56/tasks · /video-formats/46 · /tasks/119/guide → 200
POST /shortform-sessions/{id}/recommendations/next → 200 (다른 추천도 정상)
```

---

## 4. ⚪ 범위 확인

### 4-1. 유료 플랜 — MVP 인가요?

시안에 요금제가 구체적으로 설계돼 있습니다.

| | Free | Pro (₩9,900/월) |
| --- | --- | --- |
| 월 숏폼 제작 | 3개 | 무제한 |
| 워터마크 제거 | ❌ | ✅ |
| 매장 인사이트 분석 | ❌ | ✅ |

**지금은 화면만 만듭니다.** "Pro로 업그레이드" 는 비활성 + "준비 중입니다" 입니다.

**Q4-1.** MVP 범위라면 아래가 필요합니다.
- 현재 플랜 + 이번 달 남은 횟수 조회 (설정의 `Free 플랜 · 이번 달 3/3 사용 중`)
- 3개 소진 시 4.1 생성 차단 (에러 코드는?)
- 워터마크 분기 (15.1 출력)
- 인사이트 접근 제어 (3.5 를 Free 는 못 보게)
- 결제 연동

### 4-2. 기존 대기 항목

| 항목 | 질문 | 영향 |
| --- | --- | --- |
| 16.1 연동 | SNS 연동 **없이도** 17.x 성과 수집이 되나요? | 작업량 최대 분기점. 안 되면 성과 화면 전체가 연동 뒤로 밀립니다 |
| R11 직접 녹음 | MVP 범위인가요? | 현재 화면 없음 |
| 4.2 `success_metric` | 자유 입력인가요 선택지인가요? | 선택지면 목록 필요 |
| 5.1/5.2 썸네일 | YouTube 가 아닌 포맷의 썸네일은 어떻게 얻나요? | 지금은 YouTube 만 나옵니다 |
| 비밀번호 재설정 | Phase 2 로 올려주실 수 있나요? | 현장조사 시작하면 바로 필요. 지금은 이메일 문의 안내로 대체 |
| 아이디 로그인 | 회원가입 첫 칸이 시안대로 "아이디" 인데 로그인은 이메일입니다. 아이디 컬럼을 만드실 건가요? | 지금은 아이디를 `name` 으로 보내고 있어 마이페이지에 **이름 대신 아이디**가 표시됩니다 |

---

## 5. ✅ 이번에 닫힌 것 (2026-08-26 실측으로 확인)

| 항목 | 확인한 것 |
|---|---|
| **7.1 기획 생성이 500** | **200 으로 정상화.** `shooting_summary` + `scenes_preview` 옵니다 |
| **촬영 체인 전체** | 7.1 → 8.1 **태스크 5개** → 9.1 guide **200** 까지 뚫렸습니다 |
| **R06 추천 생성 503** | 정상. 대화가 끝까지 돌고 **추천 3건**이 옵니다 |
| **추천을 지정해 채택 가능한가** | **됩니다.** `accept` body 의 `recommendation_id` 를 서버가 존중합니다 (2회 교차 검증: [0] 지정 → [0], [1] 지정 → [1]) |
| **5.1 배지가 전부 null** | **전부 채워졌습니다.** `format_type`·`expected_duration_sec`·`shooting_difficulty`·`requires_face` 3/3 |
| **`guide_video_url` 요청** | 들어왔고 **대표와 다른 주소**입니다. 감사합니다 |
| **5.3 찜 응답 키** | 명세대로 `video_format_id` 확인 |
| **15.2 숏폼 제목** | `project_title` 이 4.1·4.3·15.2 에 추가된 것 확인 |
| **3.1 PATCH 허용 필드** | 서버는 다 받습니다 — 명세 문서만 갱신 필요 (§2-5 로 이관) |
| **2.1 좌표** | 정상. 지도가 안 뜨던 건 타일 서버 쪽이었고 프론트에서 해결했습니다 |
| **15.1 음원 메타데이터** | **들어왔습니다.** `publish_kit.track { mode·title·artist·start_sec·end_sec·mood }` — 내보내기 화면에 붙였습니다 (확인 질문 하나만 §2-6) |
| **R06 "다른 추천 보기"** | `POST /recommendations/next` **200**, 매번 다른 구성이 옵니다 |
| **R06 확인 단계** | `action: "CONFIRM"` 을 서버가 내려 주어 앱이 그것으로 판단합니다. 선택지 id 는 매번 달라져도(`confirm`/`confirm_recommend`) 문제없습니다 |

---

## 부록 A. 프론트가 지키고 있는 것

- 없는 값은 `—`. **0 으로 쓰지 않습니다** (0 은 "실제로 0" 이라는 주장이라서)
- `AI추론` 출처는 "AI 추측" 배지로 실제 데이터와 구분
- 저장(POST/PATCH/DELETE)은 상태를 **실제로 바꾸고**, 재조회 반영까지 확인합니다
- 화면 코드에 URL·snake_case·hex 색상을 쓰지 않습니다
- 시안에 그려져 있어도 **서버가 안 주는 값은 비웁니다.** 지어내면 사장님이 그 값을
  믿고 촬영 계획을 세웁니다 (§2-1 의 해시태그·영상이 그 사례입니다)

---

## 부록 B. 편집 완료 푸시 알림 — FE 회신 (2026-08-26)

`FE_NOTICE_2026-08-26-02.md` 에 대한 답입니다.

**결론: 붙일 수 있습니다.** 다만 공지에 안 적힌 **전제 조건 두 가지**가 실제로는
표에 적힌 FE 항목들보다 큰 일이라, 그것부터 정해야 합니다.

### E-1. 물어보신 네 가지에 대한 답

**① Expo 기반이 맞습니다.** Expo SDK 57(`expo ~57.0.14`)이고
`expo-file-system`·`expo-linking`·`expo-constants` 를 쓰고 있습니다.
`expo-notifications` 로 붙는 게 맞습니다(아직 설치 전).

**② 권한 요청 시점 — "편집을 처음 시작할 때" 를 제안합니다.**

앱 첫 실행에 물으면 사장님은 왜 필요한지 모른 채 거절합니다. 한 번 거절하면
안드로이드는 다시 묻기 어렵습니다. 편집 버튼을 누른 직후
"10분쯤 걸려요. 다 되면 알려드릴까요?" 로 물으면 맥락이 있어 수락률이 높습니다.
앱에 `07_권한 안내` 화면이 이미 있어 설명을 얹을 자리도 있습니다.

**③ 권한 거부 폴백 — 지금 동작이 그대로 폴백입니다.**
화면을 켜둔 채 14.2 를 1초마다 조회하고, 상한은 합의대로 **15분**으로 올려 뒀습니다
(`RenderScreen.tsx`). 추가 작업이 필요 없습니다.

**④ 알림 탭 → 이동할 화면**

```
편집 완료 알림  →  Create > EditResult   params: { projectId }
```

**payload 에는 `shorts_project_id` 하나면 충분합니다.**
`video_output_id` 는 그 화면이 15.1 로 다시 조회합니다.
딥링크 주소는 `reals://project/{shorts_project_id}/result` 정도를 생각하고 있습니다 —
형식은 BE 편한 쪽에 맞추겠습니다. 앱 scheme 은 `reals` 입니다.

### E-2. 🔴 공지에 없는 전제 두 가지

**① 안드로이드 푸시는 Firebase(FCM) 설정이 있어야 합니다.**

Expo Push 는 안드로이드에서 결국 FCM 을 거칩니다. 그래서 다음이 선행돼야 합니다.

- Firebase 프로젝트 생성
- `google-services.json` 을 받아 `app.json` 의 `android.googleServicesFile` 에 연결
- EAS 자격증명에 FCM V1 서비스 계정 키 등록

지금 `app.json` 에는 `googleServicesFile` 도 `notification` 설정도 없습니다.
**이건 네이티브 설정이라 OTA 로 못 가고 APK 를 다시 만들어야 합니다.**

👉 **Firebase 프로젝트를 누가 만들지 정해 주세요.** 서버 발송 쪽에서도 같은 프로젝트의
   자격증명이 필요하니 BE 가 만들고 `google-services.json` 만 주시는 쪽이 깔끔합니다.

**② 딥링크 라우팅이 아직 없습니다.**

앱 scheme(`reals`)은 잡혀 있지만 `NavigationContainer` 에 `linking` 설정이 없습니다.
지금 상태로는 알림을 눌러도 **앱만 열리고 화면 이동은 안 됩니다.**
이건 FE 몫이고 JS 라 OTA 로 나갈 수 있습니다.

### E-3. FE 쪽에서 같이 정리할 것 (참고)

**① 편집 화면에 다시 들어가면 14.1 을 또 호출합니다.**
`RenderScreen` 이 마운트될 때마다 편집을 겁니다. "앱을 꺼도 된다" 는 전제가 생기면
사장님이 편집 화면으로 돌아올 일이 많아지는데, 그때마다 렌더가 새로 걸립니다.
**푸시보다 이걸 먼저 고치겠습니다.** 진행 중이면 새로 걸지 않고 붙기만 하도록요.
→ BE 쪽에 확인 부탁: **이미 PROCESSING 인 프로젝트에 14.1 을 다시 호출하면
   서버는 어떻게 동작하나요?** (새 렌더를 또 만드는지, 기존 것을 돌려주는지)

**② 알림 문구를 시안 원문으로 되돌릴 수 있게 됩니다.**
지금 `25_알림` 화면은 시안의 "푸시 알림이 꺼져 있어요" 를 쓰지 못하고
"알림을 켜두면 놓치지 않아요" 로 바꿔 뒀습니다 — 권한 상태를 읽을 라이브러리가
없어서였습니다(부록 B 의 "가짜 데이터 금지" 항목). `expo-notifications` 가 들어오면
시안 문구를 그대로 쓸 수 있습니다.

**③ 알림 화면을 되살릴지 정해야 합니다.**
시안 6차에서 `notifications` 화면이 라우터에서 빠져 오늘 진입 경로를 닫았습니다
(화면 코드는 남겨 뒀습니다). 푸시가 들어오면 **켜고 끄는 자리**가 필요해집니다.

### E-4. 순서 제안

```
1. Firebase 프로젝트 + google-services.json        ← 담당 정해야 함 (BE 권장)
2. expo-notifications 설치 · app.json 설정          ← 네이티브, APK 재빌드 필요
3. 딥링크 linking · 토큰 발급/전송 · 알림 탭 처리    ← FE, OTA 로 나감
4. RenderScreen 재요청 정리                          ← FE, 지금도 가능
```

BE 가 토큰 저장 API 와 발송 타이머를 먼저 준비하실 수 있다는 데 동의합니다.
FE 는 4번을 먼저 하고, 1번이 정해지면 2~3번을 한 번의 APK 빌드에 묶겠습니다
(마침 앱 아이콘 교체분도 그 빌드를 기다리고 있습니다).

오늘 시연에 넣지 않는 것도 동의합니다 — 상한 15분 연장은 이미 반영돼 있습니다.

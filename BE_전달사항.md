# BE 전달사항

**2026-08-26 (밤) 전면 재작성.** 해결된 항목은 전부 걷어내고 **지금 남아 있는 것만**
남겼습니다. 적힌 상태코드·응답 키·필드 이름은 모두 **실서버를 직접 호출한 실제
응답**입니다. 추측으로 적은 줄은 없습니다.

```
확인 계정   realsqa0826@test.co.kr   (user 25)
가게        스타벅스 한국프레스센터점 (store 21)
```

---

## 0. 한눈에

| | 항목 | 한 줄 |
|---|---|---|
| 🔴 | **16.1 인스타 `client_id` 가 페이스북 App ID** | 연동이 여기서 막힙니다. 서버 값 하나만 바꾸면 됩니다 (§1-1) |
| 🔴 | **2.3 가져오기가 `PENDING` 에서 안 넘어감** | 메뉴·사진·상권분석이 전부 비어 있습니다 (§1-2) |
| 🔴 | **5.1 포맷이 3건뿐** | 홈 피드가 세 편으로 끝납니다 (§1-3) |
| 🟡 | 추천에 `video_format_id` 가 없음 | 추천 카드의 태그·영상을 못 채웁니다 (§2-1) |
| 🟡 | 2.1 에 **지번 주소**가 없음 | 매장 확인 시트의 한 줄이 빕니다 (§2-2) |
| 🟡 | 참고 영상 **`start_ms`·`end_ms`** 가 API 에 없음 | 구간 반복을 못 붙입니다. 앱은 준비 끝 (§2-3) |
| 🟡 | **`guide_type: DANCE` 가 한 번도 안 옴** | 안무 전용 화면이 도달 불가입니다 (§2-4) |
| 🟡 | 2.1 에 `kakao_place_id` 가 없음 | §1-2 와 얽혀 보입니다 (§2-5) |
| 🟡 | 15.1 `track.mode` 해석 확인 | 화면이 mode 로 갈립니다 (§2-6) |
| 🟡 | 6.1·6.2·6.3 이 404 — 폐기 확인 | 명세 정리 (§2-7) |
| 🟡 | 3.1 명세 Body 가 실제와 다름 | 서버가 맞습니다. 문서만 (§2-8) |
| 🟡 | 3.6 로고 캐시 두 가지 | 아직 못 재봤습니다 (§2-9) |
| ⚪ | 푸시 — **분담 확정.** 토큰 API 계약만 맞추면 됩니다 | §3 |
| ⚪ | 유료 플랜 MVP 범위 · 기존 대기 항목 | §4 |

---

## 1. 🔴 지금 막고 있는 것 셋

### 1-1. 16.1 인스타 연동 — `client_id` 가 **페이스북 App ID** 입니다

사장님 쪽 오류 원문입니다. **비즈니스 계정 + 페이스북 페이지 연결이 된 상태**였습니다.

```
유효하지 않은 요청: 요청 매개변수가 유효하지 않습니다:
Invalid scope: instagram_business_basic+instagram_business_manage_insights
```

처음에는 `instagram_business_manage_insights` 가 앱에 안 열린 것으로 봤는데, scope 만
바꾼 주소 4개를 시험하니 **`instagram_business_basic` 하나만 넣은 것까지 전부 막혔습니다.**
특정 권한 문제가 아니라는 뜻이라 앱 설정 쪽을 팠습니다.

**결정적 증거 — 이 id 는 페이스북 앱 다이얼로그가 받습니다.**

```
GET facebook.com/v21.0/dialog/oauth?client_id=1234567890123456   (아무 숫자)
   → /oauth/error/?error_code=PLATFORM__INVALID_APP_ID
     "앱 ID 오류: 입력된 앱 ID가 유효한 앱 ID와 다릅니다."

GET facebook.com/v21.0/dialog/oauth?client_id=1541622377157897   (지금 쓰는 id)
   → 정상. 페이스북 로그인 화면이 뜹니다 (앱 ID 오류 없음)
```

가짜 숫자는 걸러내는데 이 id 는 통과합니다 → **유효한 페이스북 App ID 입니다.**

그런데 메타 문서는 `https://www.instagram.com/oauth/authorize` 의 `client_id` 가
**"Instagram App ID" — 페이스북 App ID 가 아니라고** 명시합니다.

> App Dashboard > Instagram > API setup with Instagram login >
> 3. Set up Instagram business login > **Business login settings > Instagram App ID**

둘은 **서로 다른 숫자**입니다. 페이스북 App ID 를 인스타 authorize 에 넣으면 인스타가
그 앱의 Instagram-Login 설정을 못 찾고, 등록된 scope 가 없으니 **무슨 scope 를 넣든
`Invalid scope`** 가 납니다. 관측된 증상과 정확히 맞습니다(넷 다 실패 · 기본 권한 하나만
넣어도 실패).

- **Q1-1a. 아래 둘 중 하나로 고쳐 주세요.**

  **(가) 지금 흐름 유지 — `client_id` 만 교체 (권장)**
  위 경로에서 **Instagram App ID** 를 찾아 그 값으로 바꾸시면 됩니다. 같은 화면의
  **OAuth redirect URI** 목록에 `https://sarils.p-e.kr/sns-connections/callback` 이
  들어 있는지도 같이 확인해 주세요.

  **(나) 페이스북 로그인 방식으로 전환**
  authorize 주소를 `facebook.com/v21.0/dialog/oauth` 로 바꾸고 scope 를
  `instagram_basic` · `instagram_manage_insights` · `pages_show_list` 등으로 교체.
  지금 id 를 그대로 쓸 수 있지만 사장님이 **페이스북 계정으로** 로그인하게 됩니다.
  주 사용자가 40~60대라 (가)가 낫다고 봅니다.

  **어느 쪽이든 앱은 서버가 주는 `authorize_url` 을 열기만 하므로 프론트 수정이 없습니다.**
  서버에서 값만 바꾸면 **지금 깔린 APK 로도 바로 됩니다.**

- **Q1-1b.** scope 가 **전부 읽기 전용**입니다.

  | 플랫폼 | 서버가 실제로 요청하는 범위 |
  |---|---|
  | INSTAGRAM | `instagram_business_basic` · `instagram_business_manage_insights` |
  | YOUTUBE | `yt-analytics.readonly` · `youtube.readonly` |

  올리는 권한이 없습니다. 그렇다면 **16.2 자동 게시는 이 연동으로 불가능**하고,
  게시는 PENDING_LINK(사장님이 직접 올리고 링크 연결)만 남는 것으로 이해하면 되나요?

  > 앱 동의 화면은 시안 문구대로 "게시물 업로드 권한" 이라고 적고 있었습니다.
  > 사장님께 거짓을 말하는 셈이라 **실제 범위대로 고쳤습니다**(성과 읽기).

- **Q1-1c.** 유튜브도 같은 종류의 벽이 있습니다. `yt-analytics.readonly` 는 구글
  **민감 범위**라 OAuth 동의 화면이 미검증이면 **테스트 사용자로 등록된 계정만**
  통과합니다. 지금 게시(공개) 상태인가요, 테스트인가요?

### 1-2. 2.3 가져오기가 `PENDING` 에서 안 넘어갑니다

```
GET /stores/21/import-status
  overall_status: IN_PROGRESS
  기본정보 SUCCESS / 메뉴 PENDING / 사진 PENDING / 상권분석 PENDING

GET /stores/21/menus             → { "menus": [] }
GET /stores/21/target-customers  → { "target_customers": [] }
GET /stores/21/photos            → { "photos": [] }
```

2.1 검색으로 등록한 **네이버 유명 프랜차이즈 지점**인데도 셋 다 비어 있습니다.
등록 직후부터 여러 시간이 지나도, 오늘 밤 다시 재도 같습니다.

**막히는 것:** AI 추천 대화가 "어떤 메뉴를 홍보할까요?" 를 묻는데 고를 것이 없습니다.

> **API 자체는 멀쩡합니다.** 오늘 앱에서 메뉴를 직접 넣고 고치고 지워 봤고
> POST 201 · PATCH 200 · DELETE 200 전부 정상이었습니다. **막힌 것은 수집기 쪽**입니다.
>
> 프론트는 사장님이 직접 넣는 길을 열어 뒀습니다(매장 정보 수정 → 매장 메뉴 관리).
> 다만 자동 수집이 되는 게 원래 설계라 그대로 두면 안 될 것 같습니다.

### 1-3. 5.1 포맷이 **3건**뿐입니다

```
2026-08-26 오전    GET /video-formats?sort=trending  → 20건
2026-08-26 오후·밤  GET /video-formats               → 3건
                   GET /video-formats?sort=trending → 3건
                   GET /video-formats?sort=latest   → 3건
```

정렬을 바꿔도 3건입니다. 홈 피드가 세 편으로 끝나고 관심 목록도 그 안에서만 고릅니다.

**의도한 것인가요?** 트렌드 클러스터를 새로 도는 중이라 잠깐 줄어든 것이라면 언제
복구되는지, 3건이 정상이라면 그 이유를 알려주세요.

---

## 2. 🟡 확인 부탁드리는 것

### 2-1. 추천 응답에 `video_format_id` 가 없습니다

R06 추천이 주는 것은 이게 전부입니다.

```
recommendation_id · project_title · title · concept
editing_template_id · editing_template_version
```

시안의 추천 카드는 **해시태그 3개 + 숏츠 영상 임베드**가 함께 있습니다. 그 둘을 채울
값이 없어 지어내지 않고 비워 뒀습니다 — 지금 카드는 제목·요약·버튼뿐입니다.

- **Q2-1.** 추천 단계에서 `video_format_id` 를 함께 주실 수 있나요?
  그러면 나머지(썸네일·촬영시간·난이도·얼굴노출)는 프론트가 5.2 로 스스로 채웁니다.
  포맷 번호는 지금 **채택한 뒤에야** 응답으로 돌아옵니다.

### 2-2. 🆕 2.1 검색에 **지번 주소**를 넣어 주실 수 있나요?

시안 8차의 매장 확인 시트는 주소를 두 줄로 보여줍니다.

```
서울 동작구 보라매로 87
지번 신대방동 395-69      ← 이 줄
```

그런데 2.1 응답에 지번이 없어 **줄 자체를 안 그렸습니다**(지어내지 않기).

```
GET /stores/search?keyword=스타벅스
응답 키: source, name, address, phone, latitude, longitude,
        category, distance_m, external_channel_url
```

- **Q2-2a.** `jibun_address` 를 추가해 주실 수 있나요? 40~60대 사장님은 도로명보다
  지번으로 자기 가게를 확인하는 경우가 많아, "내 가게가 맞나" 를 가르는 데 실제로
  쓰이는 값입니다. 내려오면 앱은 그 자리에 한 줄 추가만 하면 됩니다.
- **Q2-2b.** `phone` 이 **자주 `null`** 입니다(스타벅스 한국프레스센터점 실측).
  앱은 값이 있을 때만 전화 줄을 그립니다. 네이버에 번호가 있는데 안 오는 것이라면
  같이 봐 주세요.

> 참고 — `category` 는 `"카페,디저트>카페"` 처럼 분류 경로가 통째로 옵니다. 프론트가
> 우리 업종 칩으로 옮기고 있습니다. 서로 다른 카테고리 원문 59종을 뽑아 규칙을
> 맞췄고 47종 중 38종이 자동 분류됩니다. **이건 저희가 처리하니 바꾸지 않으셔도 됩니다.**

### 2-3. 🆕 참고 영상 구간 `start_ms` · `end_ms` 를 **API 로 내보내 주세요**

촬영 화면의 유튜브 참고 영상을 **지금 찍을 컷의 구간만** 반복 재생하려고 합니다.
지금은 영상 전체가 처음부터 돌아서, 사장님이 "내가 따라할 동작" 이 몇 초에 나오는지
직접 찾아야 합니다.

**값이 영상편집 DB 에 `start_ms`·`end_ms` 로 있다고 전달받았습니다. 그런데 API 로는
하나도 안 나옵니다.**

```
openapi.json 전문 검색       start_ms 0건 · end_ms 0건 · *_ms 0건
GET /video-formats/46        구간 없음
GET /shorts-projects/56      구간 없음
GET /tasks/119/guide         구간 없음 (reference_video 는 null)
GET /shorts-projects/56/scenes  구간 없음 (target_duration_sec 만)
```

서버가 개념을 갖고 있는 흔적은 응답에 그대로 남아 있습니다.

```
scenes[].shot_type   = "가이드 구간 재현"     ← 구간을 재현하라고 적혀 있음
broll_shot.shot_type = "가이드 구간 재현"
scenes[].target_duration_sec = 3             ← 길이만 있고 시작점이 없음
```

- **Q2-3a. 태스크(또는 씬)마다 `start_ms`·`end_ms` 를 실어 주세요.**
  9.1 `GET /tasks/{id}/guide` 가 가장 자연스럽습니다 — 촬영 화면이 컷마다 부르는
  바로 그 호출입니다.

  ```
  GET /tasks/119/guide
  {
    "guide_type": "OVERLAY",
    "reference_segment": { "start_ms": 4500, "end_ms": 7500 },
    ...
  }
  ```

- **Q2-3b.** 구간의 기준 영상이 무엇인가요? `reference_video` 가 지금 **항상 `null`**
  이라 앱은 포맷의 `guide_video_url` 을 틉니다. 구간도 그 영상 기준인가요?
  기준이 다르면 구간 값과 함께 그 영상 주소도 주셔야 합니다.
- **Q2-3c.** `end_ms` 없이 `start_ms` 만 오는 경우가 있나요? 그러면
  `target_duration_sec` 을 더해 끝을 계산하겠습니다.

#### 앱 쪽은 준비돼 있습니다 — 값만 오면 한 줄입니다

값이 없어 못 붙일 뿐, 기술 검증은 끝냈습니다. `guidePlayerBridge.ts` 를 컴파일해
**실제 우리 코드가 만든 문서**를 띄우고 재봤습니다.

```
컷1  구간  1.0~3.0s  → 실제 1.00 ~ 3.14   벗어남 0회
컷2  구간  5.0~8.0s  → 실제 5.00 ~ 8.20   벗어남 0회
구간 끄기            → 5.00 ~ 12.78 (계속 흘러감 = 정상)
컷 전환 중 iframe 재로딩 0회 (컷 사이에 검은 화면이 없습니다)
```

끝점을 0.1~0.2초 지나쳤다 되감깁니다 — 유튜브가 위치를 ~0.25초 간격으로 알려주기
때문이고 더 줄일 방법이 없습니다.

### 2-4. 🆕 `guide_type: DANCE` 가 **한 번도 안 옵니다**

앱에는 안무 전용 촬영 화면이 있습니다. 참고 영상을 보면서 따라 추는 자리라 일반
촬영과 화면이 다릅니다. 그런데 거기로 가는 조건이 `guide_type === 'DANCE'` 인데
**그 값이 한 번도 오지 않습니다.**

**챌린지 포맷으로 직접 태워 봤습니다.**

```
POST /shorts-projects            video_format_id 4 "오츠카레 썸머 챌린지" (format_type 챌린지)
POST /shorts-projects/58/plan    200
GET  /shorts-projects/58/tasks   5개 — 전부 task_type "영상촬영"
       125 오버헤드 V 포즈 훅
       126 와이드 안무 A → 오버헤드 포인트
       127 와이드 안무 B → 오버헤드 포인트
       128 와이드 안무 C → 마지막 오버헤드 전환
       129 오버헤드 근접 마지막 포즈

GET /tasks/125|126|127/guide → 전부 guide_type "OVERLAY" · reference_video null
```

**컷 제목에 "안무" 라고 적혀 있는데도 `OVERLAY` 입니다.** 그래서 안무를 일반 촬영
화면에서 찍게 되고, 전용 화면은 코드만 남아 있습니다.

- **Q2-4a.** `DANCE` 를 실제로 내려 줄 계획인가요, 폐기됐나요? 폐기라면 앱에서 그
  화면과 분기를 걷어내겠습니다 — 지금처럼 두면 "언젠가 되는 기능" 으로 착각한 채 남습니다.
- **Q2-4b.** DANCE 일 때 `reference_video` 는 채워지나요? 스키마 설명이 "댄스·안무
  가이드용 원본 영상" 인데 지금은 항상 `null` 입니다.
- **Q2-4c.** **안무 순서**를 줄 계획이 있나요? 시안에는 촬영 전에 순서를 익히는
  화면이 있는데(`1-8박 인트로 포인트`, `9-16박 메인 안무` …) 그건 시안 목업이고
  서버에 대응하는 값이 없습니다(명세 전문에 `choreo` 0건). **채울 값이 없어서 그
  화면은 만들지 않았습니다.** 계획이 있다면 어떤 모양인지 알려주세요.

### 2-5. 2.1 검색 응답에 `kakao_place_id` 가 없습니다

명세 2.1 에는 있다고 되어 있어 프론트가 2.2 등록 때 그대로 돌려보내도록 만들어
뒀는데 **받을 값이 없어 항상 null 로 나갑니다.** 대표메뉴 자동 수집 트리거가 동작하지
않을 텐데(§1-2 와 관련 있어 보입니다), 명세에서 빼는 게 맞는지 서버에 추가하실 건지
알려주세요.

> 좌표는 정상입니다 (`latitude: 37.5672475`, `longitude: 126.9780493`).

### 2-6. 15.1 `track.mode` 해석만 확인 부탁드립니다

요청드린 음원 필드가 그대로 들어왔습니다. 감사합니다 — 내보내기 화면에 붙였습니다.

```
PublishKit → caption · hashtags · post_note · track
TrackInfo  → mode · title · artist · start_sec · end_sec · mood
```

`mode` 에 따라 화면이 갈리게 만들었는데, 읽은 뜻이 맞는지만 봐 주세요.

| mode | 앱이 그리는 것 | 저희가 이해한 뜻 |
|---|---|---|
| `FIXED` | "곡명 - 아티스트" + 사용 구간 | 이 곡을 쓰면 된다 |
| `SUGGESTED` | "어울리는 음원" + `mood` | 곡은 못 정했고 분위기만 준다 |

- **Q2-6.** 맞나요? `SUGGESTED` 일 때 `title` 이 `null` 로 오는 게 정상인가요?
  (그때 곡명 카드를 그리면 빈 카드가 됩니다. 지금은 분위기만 보여줍니다.)
  `start_sec`·`end_sec` 은 **원곡에서의 위치**로 이해했습니다 — 완성 영상 기준이 아니라요.

### 2-7. 6.1 · 6.2 · 6.3 은 폐기된 것으로 이해했습니다 — 명세에서 지워 주세요

```
GET  /shorts-projects/{id}/quiz-questions  → 404 (오늘 밤 재확인)
POST /shorts-projects/{id}/quiz-answers    → 404
```

R06 이 대화형 에이전트(`/shortform-sessions/…`)로 바뀌면서 없어진 것으로 보입니다.
맞다면 명세에서 지워 주세요. 프론트는 이 경로를 "서버에 아직 없는 것" 으로 보고 앱
안에서 흉내 내는 장치를 두고 있는데, 확답을 받으면 걷어내겠습니다.

### 2-8. 3.1 명세 Body 가 실제 서버와 다릅니다 (서버가 맞습니다)

명세 3.1 PATCH 의 Body 예시는 `business_hours`·`brand_tone`·`brand_color` 셋뿐인데
**실제 서버는 더 받습니다.** 전부 200 이고 값도 저장됩니다.

```
PATCH /stores/21  { name, category }                           → 200, 저장됨
PATCH /stores/21  { business_hours, brand_tone, brand_color }   → 200, 저장됨
PATCH /stores/21  { address, phone }                            → 200, 저장됨
```

프론트는 문제없이 쓰고 있습니다. **명세 문서만 실제에 맞게 갱신해 주세요.**

### 2-9. 3.6 로고 업로드 — 캐시 두 가지 (아직 못 재봤습니다)

- **Q2-9a.** 업로드 성공 뒤 `GET /stores/{id}` 를 다시 부르면 **즉시** 갱신된 URL 이
  오나요? CDN 캐시로 잠깐 예전 이미지가 올 수 있나요?
  (지연된다면 프론트가 응답의 `logo_url` 을 낙관적으로 먼저 보여주겠습니다)
- **Q2-9b.** 파일명이 매번 바뀌나요, 같은 URL 에 덮어쓰기인가요? 같은 URL 이면 앱이
  예전 이미지를 캐시합니다. (그 경우 `updated_at` 을 쿼리로 붙이겠습니다)

---

## 3. ⚪ 편집 완료 푸시 — **분담 확정. 토큰 API 계약만 맞추면 됩니다**

보내주신 정정이 **맞습니다.** Expo 발송 서비스를 쓰면 BE 는 Firebase 자격증명을
만질 일이 없고, 키는 EAS(프론트 계정)에만 등록하면 됩니다. **Firebase 프로젝트는
프론트가 만들겠습니다.** 이전 회신에서 "BE 가 만들어 주시는 쪽이 깔끔합니다" 라고
적었던 것은 FCM 으로 직접 쏘는 구조를 전제한 것이라 **틀린 권고였습니다. 정정합니다.**

```
앱   Firebase 프로젝트 · google-services.json · EAS 에 FCM 키 등록 · APK 재빌드
     알림 권한 요청 → ExponentPushToken 발급 → 저장 API 로 전송 → 딥링크 라우팅
BE   토큰 저장 API · 편집 상태 주기 확인 · Expo 로 발송 요청
```

딥링크는 제안하신 대로 **`reals://project/{shorts_project_id}/result`** 로 하겠습니다.
앱 scheme 은 `reals` 입니다.

- **Q3-a. `POST /users/me/push-tokens` 의 Body 를 정해 주세요.** 저희 제안입니다.

  ```
  { "token": "ExponentPushToken[xxxxxxxx]", "platform": "ANDROID" }
  ```

  `platform` 이 필요 없으면 빼겠습니다.

- **Q3-b. 같은 토큰을 여러 번 보내면** 어떻게 되나요? 앱은 실행할 때마다 토큰을
  확인해서 바뀌었으면 보내려고 합니다. 서버가 알아서 합치면(upsert) 앱은 매번 보내면
  되고, 아니면 앱이 마지막으로 보낸 값을 기억했다가 달라졌을 때만 보내겠습니다.
- **Q3-c. 로그아웃·계정 삭제 때** 토큰을 지우는 경로가 필요한가요?
  (`DELETE /users/me/push-tokens/{token}` 같은 것)
- **Q3-d. 알림 payload 에 넣어 주실 값**은 `shorts_project_id` 하나면 충분합니다.
  `video_output_id` 는 그 화면이 15.1 로 다시 조회합니다.

> **14.1 중복 호출도 함께 고쳐 주신다니 감사합니다.** 앱 쪽은 이미 막아 뒀습니다 —
> `PENDING`·`PROCESSING`·`COMPLETED` 인 프로젝트에는 14.1 을 다시 걸지 않고 붙기만
> 합니다. 서버가 멱등하게 동작하면 다른 경로(재시도·푸시 복귀)에서 겹쳐 들어가도
> 안전해집니다.

⚠️ **이번 주 안에 붙지는 않습니다.** 네이티브 설정이라 APK 를 다시 만들어야 하는데,
빌드 횟수를 아끼려고 내보내기 공유(`expo-sharing`)·앱 아이콘 교체와 **한 번에 묶을**
계획입니다. BE 쪽 토큰 저장 API 는 먼저 만들어 두셔도 됩니다.

---

## 4. ⚪ 범위 확인 (이전 질문 유지)

### 4-1. 유료 플랜 — MVP 인가요?

시안에 요금제가 구체적으로 설계돼 있습니다(Free 월 3개 / Pro ₩9,900 무제한 ·
워터마크 제거 · 인사이트). **지금은 화면만 만듭니다** — "Pro로 업그레이드" 는 비활성 +
"준비 중입니다" 입니다.

**Q4-1.** MVP 범위라면 아래가 필요합니다.
- 현재 플랜 + 이번 달 남은 횟수 조회
- 3개 소진 시 4.1 생성 차단 (에러 코드는?)
- 워터마크 분기 (15.1 출력)
- 인사이트 접근 제어 (3.5 를 Free 는 못 보게)
- 결제 연동

### 4-2. 기존 대기 항목

| 항목 | 질문 | 영향 |
|---|---|---|
| 16.1 연동 | SNS 연동 **없이도** 17.x 성과 수집이 되나요? | 작업량 최대 분기점. 안 되면 성과 화면 전체가 연동 뒤로 밀립니다 |
| R11 직접 녹음 | MVP 범위인가요? | 현재 화면 없음 |
| 4.2 `success_metric` | 자유 입력인가요 선택지인가요? | 선택지면 목록 필요 |
| 5.1/5.2 썸네일 | YouTube 가 아닌 포맷의 썸네일은 어떻게 얻나요? | 지금은 YouTube 만 나옵니다 |
| 비밀번호 재설정 | Phase 2 로 올려주실 수 있나요? | 지금은 이메일 문의 안내로 대체 |
| 아이디 로그인 | 회원가입 첫 칸이 시안대로 "아이디" 인데 로그인은 이메일입니다. 아이디 컬럼을 만드실 건가요? | 지금은 아이디를 `name` 으로 보내고 있어 마이페이지에 **이름 대신 아이디**가 표시됩니다 |
| 홈 릴스 채널 프로필 | 유튜브 채널 썸네일(`channel_thumbnail_url`)을 주실 수 있나요? | 지금은 빈 동그라미입니다. YouTube Data API 키 없이는 프론트가 못 구합니다 |

---

## 5. ⚪ 저희가 아직 못 재본 것

| 항목 | 왜 못 했나 |
|---|---|
| **15.1 · 15.2 실물** | 렌더가 끝난 프로젝트가 있어야 하는데, 그러려면 촬영본을 올려야 합니다. 스키마는 확인했고(§2-6) **실제 값이 어떻게 채워져 오는지**를 못 봤습니다 |
| **인스타·구글 실제 로그인** | 저희에게 그 계정이 없습니다. authorize 주소가 로그인 화면까지 여는 것은 확인했고, 그 뒤는 §1-1 이 고쳐진 뒤 사장님 계정으로 봐야 합니다 |
| **3.6 로고 캐시** | §2-9 |
| **푸시 전 과정** | Firebase·APK 재빌드가 선행입니다 (§3) |

> 참고로 **대화 → 추천 → 채택 → 촬영 준비까지는 오늘 실제로 태웠습니다.**
>
> ```
> POST /stores/21/shortform-sessions                 201
> POST /shortform-sessions/{id}/turns × 8             200  (선택지 → 확인 → 추천 3건)
> POST /shortform-sessions/{id}/accept                201  (project 56)
> GET  /shorts-projects/56/tasks · /video-formats/46 · /tasks/119/guide   200
> POST /shortform-sessions/{id}/recommendations/next  200  (다른 추천도 정상)
> ```

---

## 부록. 프론트가 지키고 있는 것

- 없는 값은 `—`. **0 으로 쓰지 않습니다** (0 은 "실제로 0" 이라는 주장이라서)
- `AI추론` 출처는 "AI 추측" 배지로 실제 데이터와 구분
- 저장(POST/PATCH/DELETE)은 상태를 **실제로 바꾸고**, 재조회 반영까지 확인합니다.
  오늘 "저장 실패인데 성공했다고 말하던" 곳을 하나 찾아 고쳤습니다
- 화면 코드에 URL·snake_case·hex 색상을 쓰지 않습니다
- **시안에 그려져 있어도 서버가 안 주는 값은 비웁니다.** 지어내면 사장님이 그 값을
  믿고 촬영 계획을 세웁니다 — §2-1 의 해시태그·영상, §2-2 의 지번, §2-4 의 안무
  순서가 그래서 비어 있습니다

# [BE 전달] Reals 프론트엔드 — 확인·요청 사항

작성일: 2026-08-23
기준: 프론트 `v8` · 명세 `ExportBlock-60e70a8d`(2026-08-23 갱신본)

이 문서는 **갱신된 명세를 프론트에 반영하면서 실제로 막히거나 어긋난 것**만 모았습니다.
추측이 아니라 명세 원문과 프론트 코드를 대조한 결과입니다.

---

## 0. 요약

| # | 구분 | 항목 | 급한 정도 |
| --- | --- | --- | --- |
| 1 | 🔴 불일치 | 3.1 `PATCH /stores/{storeId}` 허용 필드 — 프론트가 명세에 없는 필드를 보내고 있음 | 높음 |
| 2 | 🔴 불일치 | 5.3 찜 토글 응답 키 (`format_id` vs `video_format_id`) | 낮음(프론트에서 맞춤) |
| 3 | 🟡 요청 | 15.2 숏폼에 **제목**이 없음 — 뷰어 화면에 표시할 것이 없음 | 중간 |
| 4 | 🟡 요청 | 15.1 **음원 메타데이터**(곡명·아티스트·사용 구간) 필드 없음 | 중간 |
| 5 | 🟡 질문 | 3.6 로고 업로드 후 3.1 캐시 무효화 시점 | 낮음 |
| 6 | 🟡 질문 | 16.1 OAuth — 브라우저 복귀 감지 방식과 실패 응답 | 중간 |
| 7 | ⚪ 대기 | 유료 플랜 MVP 여부 (사용량·워터마크·인사이트 접근 제어) | 낮음 |
| 8 | ⚪ 대기 | 기존 미해결 4건 (성과수집·직접녹음·success_metric·썸네일) | 낮음 |

---

## 1. 🔴 3.1 `PATCH /stores/{storeId}` — 허용 필드가 안 맞습니다

### 무엇이 문제인가

명세 3.1 PATCH 의 Body 예시는 세 필드뿐입니다.

```json
{
    "business_hours": "매일 10:00-22:00 (연중무휴)",
    "brand_tone": "친근하고 정겨운 동네 분식집 느낌",
    "brand_color": "#FF6B35"
}
```

그런데 프론트 `StoreEditScreen`(가게 정보 수정 화면)은 아래를 함께 보냅니다.

```js
{ name, category, address, phone, businessHours, brandTone, brandColor }
```

`name`·`category`·`address`·`phone` 이 명세에 없습니다.

### 왜 지금 물어보는가

4.2 에서 **"목적에 맞지 않는 키를 보내면 400"** 규칙이 있었던 것처럼, 3.1 도
허용 목록 방식이면 실서버 연결 순간 이 화면 저장이 통째로 실패합니다.
Mock 으로는 안 잡히는 유형이라(응답은 잘 옵니다) 미리 확인해야 합니다.

### 여쭙는 것

**Q1-a.** 3.1 PATCH 로 `name`·`category`·`address`·`phone` 을 수정할 수 있나요?

- **된다면** → 명세 Body 예시에 추가해 주세요. 프론트는 그대로 둡니다.
- **안 된다면** → 이 값들은 어디서 고치나요? 2.2 로 가게를 다시 등록해야 하나요?
  (사장님이 상호를 바꾸거나 이전하는 경우가 실제로 있습니다)

**Q1-b.** 허용되지 않는 키가 오면 400 인가요, 아니면 무시하고 200 인가요?
400 이면 프론트가 필드를 걸러 보내야 하고, 무시라면 그대로 둬도 됩니다.

> 참고: 같은 이유로 4.2 는 `promotion_purpose`·`video_format_id` 가 오면 400 이 나도록
> 프론트 Mock 에도 검증을 넣어 두었습니다. 3.1 도 같은 방식이면 맞추겠습니다.

---

## 2. 🔴 5.3 찜 토글 응답 키

명세(5.3 찜하기/해제)의 응답은 이렇습니다.

```json
{ "video_format_id": 71, "is_favorite": true, "created_at": "..." }
```

프론트 Mock 은 `format_id` 로 만들어 두었습니다. **프론트에서 `video_format_id` 로 맞추겠습니다.**
확인만 부탁드립니다 — 명세대로 `video_format_id` 가 맞나요?

> 나머지(찜 목록이 5.1 과 동일 구조, POST/DELETE 멱등, 계정 단위)는 프론트 구현과 일치합니다.

---

## 3. 🟡 15.2 — 숏폼에 표시할 **제목**이 없습니다

### 상황

마이페이지 그리드(3열)와 전체화면 뷰어를 만듭니다. 그리드는 커버 이미지만 쓰므로 문제 없는데,
**뷰어 하단에 넣을 제목이 없습니다.**

디자인 시안에는 이렇게 되어 있습니다.

```
[가게 아바타] 열정커피 강남점
이 치즈김밥 단면 실화?      ← 이 줄에 해당하는 필드가 없음
```

명세에도 같은 내용이 적혀 있습니다.

> 카드 라벨은 `promotion_purpose`입니다 — 프로젝트에는 제목 개념이 없습니다(4.1도 같습니다).
> 제목이 필요하면 별도 컬럼 신설이 필요하니 알려주세요.

### 여쭙는 것

**Q3.** 아래 셋 중 어느 쪽으로 갈까요?

| 안 | 내용 | 프론트 작업 |
| --- | --- | --- |
| **A** | `promotion_purpose` 를 그대로 씀 ("메뉴소개") | 지금 가능. 다만 영상마다 같은 글자가 반복됩니다 |
| **B** | 15.1 `publish_kit.caption` 첫 줄을 제목으로 재사용 | 15.2 응답에 `caption` 추가 필요 |
| **C** | `video_outputs` 에 `title` 컬럼 신설 (AI 가 생성) | BE 작업 발생 |

**당장은 A 로 만들어 두겠습니다.** B/C 로 정해지면 필드만 바꿔 끼우면 됩니다.

---

## 4. 🟡 15.1 — 음원 메타데이터 요청

### 상황

내보내기 화면 시안에는 음원 정보가 두 카드로 들어 있습니다.

```
♪ 음원 정보          Aesthetic - Tollan Kim        [복사]
🕐 음원 사용 구간     0:12 ~ 0:27 구간 사용
```

그런데 15.1 이 주는 음원 관련 정보는 두 개뿐입니다.

- `has_licensed_audio` (true/false)
- `publish_kit.post_note` (문장 — 예: "음원은 Instagram 내 '설렘' 사운드 추천")

**곡명·아티스트·사용 구간 필드가 없습니다.**

지금은 없는 값을 지어내지 않기 위해, 이 두 카드 대신
`has_licensed_audio` 기반 안내 문장 하나만 표시하고 있습니다.

### 여쭙는 것

**Q4-a.** 아래 필드를 15.1 응답에 추가해 주실 수 있나요?

```json
"publish_kit": {
    "caption": "...",
    "hashtags": ["..."],
    "post_note": "...",
    "track": {
        "title": "Aesthetic",
        "artist": "Tollan Kim",
        "start_sec": 12,
        "end_sec": 27
    }
}
```

**Q4-b.** 그게 어렵다면, `post_note` 안에 곡명을 넣어 주시는 것만으로도 됩니다.
사장님이 플랫폼에서 같은 음원을 찾을 수 있어야 하는 게 핵심입니다.

**Q4-c.** "사용 구간"이 실제로 있는 개념인가요?
AI 편집이 원곡의 특정 구간을 잘라 쓰는 구조라면 필요하고,
전체를 페이드로 쓰는 구조라면 이 카드 자체를 빼겠습니다.

---

## 5. 🟡 3.6 로고 업로드 — 확인 두 가지

신설해 주신 `POST /stores/{storeId}/logo` 는 프론트에서 바로 쓸 수 있습니다.
(multipart, 가게당 1장 교체, 415/413/400 에러코드까지 명확합니다. 감사합니다.)

**Q5-a.** 업로드 성공 응답에 `logo_url` 이 오는데, 이후 3.1 `GET /stores/{storeId}` 를
다시 부르면 **즉시 갱신된 URL** 이 오나요? CDN 캐시 때문에 잠깐 예전 이미지가 올 수 있나요?
(잠깐 지연된다면 프론트가 응답의 `logo_url` 을 낙관적으로 먼저 보여주겠습니다)

**Q5-b.** 파일명이 매번 바뀌나요, 아니면 같은 URL 에 덮어쓰기인가요?
같은 URL 이면 앱이 예전 이미지를 캐시해서 계속 보여주는 문제가 생깁니다.
(이 경우 `updated_at` 을 쿼리스트링으로 붙여 회피하겠습니다)

---

## 6. 🟡 16.1 OAuth (A방식) — 배선하며 생긴 질문

`GET /sns-connections/authorize` 신설 확인했습니다. 아래 흐름으로 붙이고 있습니다.

```
① 앱 → GET /sns-connections/authorize?platform=INSTAGRAM
② 앱 → 받은 authorize_url 을 브라우저로 열기
③ (서버가 콜백 처리 — 앱 관여 없음)
④ 앱 → 포그라운드 복귀 감지 → GET /sns-connections 로 확인
```

**Q6-a.** ④의 복귀 감지는 앱의 `AppState` 로만 판단합니다.
사장님이 **동의하지 않고 그냥 브라우저를 닫고 돌아온 경우**에도 복귀로 잡힙니다.
이때 `GET /sns-connections` 목록이 그대로면 "연동 안 됨"으로 처리하려 하는데,
그 판단이 맞을까요? 혹시 "진행 중" 같은 중간 상태가 생길 수 있나요?

**Q6-b.** `authorize_url` 발급 자체가 실패할 수 있나요?
(예: 플랫폼 설정 누락) 그렇다면 에러 코드를 알려주세요. 사장님께 보여줄 문구를 맞추겠습니다.

**Q6-c.** 연동 완료 페이지 문구는 "연결됐습니다. 앱으로 돌아가 주세요." 로 확정된 것으로 알고 있습니다.
맞나요? 앱 쪽 안내 문구를 여기에 맞추겠습니다.

**Q6-d.** 배포 도메인이 정해지면 알려주세요. 실제 연동 테스트의 선행 조건입니다.
지금은 Mock 으로 "바로 연동됨" 상태를 만들어 이후 화면까지 검증해 두었습니다.

---

## 7. ⚪ 유료 플랜 — MVP 인지 확인

디자인 시안에 요금제가 구체적으로 설계돼 있습니다.

| | Free | Pro (₩9,900/월) |
| --- | --- | --- |
| 월 숏폼 제작 | 3개 | 무제한 |
| 워터마크 제거 | ❌ | ✅ |
| 매장 인사이트 분석 | ❌ | ✅ |

**지금은 화면만 만듭니다.** "Pro로 업그레이드" 버튼은 비활성 + "준비 중입니다" 표시입니다.

**Q7.** MVP 범위인가요? 범위라면 아래가 필요합니다.

- 현재 플랜 + 이번 달 남은 횟수 조회 (설정 화면의 `Free 플랜 · 이번 달 3/3 사용 중`)
- 3개 소진 시 4.1 프로젝트 생성 차단 (402? 403? 에러 코드)
- **워터마크 분기** — 15.1 출력에 플랜별 처리
- **인사이트 접근 제어** — 3.5 를 Free 는 못 보게
- 결제 연동

---

## 8. ⚪ 기존 대기 항목 (재확인)

이전에 여쭙고 아직 답을 못 받은 것들입니다. 지금 막히지는 않습니다.

| 항목 | 질문 | 영향 |
| --- | --- | --- |
| 16.1 연동 | SNS 계정 **연동 없이도** 17.x 성과 수집이 되나요? | 작업량 최대 분기점입니다. 안 되면 성과 화면 전체가 연동 뒤로 밀립니다 |
| R11 직접 녹음 | MVP 범위인가요? | 현재 화면 없음 |
| 4.2 `success_metric` | 자유 입력인가요 선택지인가요? | 선택지면 목록 필요 |
| 5.1/5.2 썸네일 | YouTube 가 아닌 포맷(Instagram·TikTok)의 썸네일은 어떻게 얻나요? | 현재 YouTube 만 썸네일이 나옵니다 |
| 7.1 AI 연동 | 예상 시점? | 지금은 임시 뼈대라 가게와 무관한 기획이 나옵니다 |
| 비밀번호 재설정 | Phase 2 로 올려주실 수 있나요? | 현장조사 시작하면 바로 필요해집니다. 지금은 로그인 화면에 이메일 문의 안내로 대체 |

---

## 부록 A. 이번 명세 갱신분 반영 상태

| 명세 | 내용 | 프론트 |
| --- | --- | --- |
| 16.1 `GET /sns-connections/authorize` | OAuth A방식 (POST 제거) | 반영 예정 |
| 3.6 `POST /stores/{storeId}/logo` | 가게 로고 업로드 | 반영 예정 |
| 15.2 `GET /stores/{storeId}/shorts` | 완성 숏폼 그리드 | 반영 예정 |
| 1.5 `GET·PATCH /users/me` | 회원정보 조회·수정 | 반영 예정 |
| 5.3 찜 3종 | 목록·찜하기·해제 | **이미 반영됨** (응답 키만 조정) |

## 부록 B. 프론트가 지키고 있는 것

- 없는 값은 `N/A`. **0 으로 쓰지 않습니다** (0 은 "실제로 0" 이라는 주장이라서)
- `AI추론` 출처는 "AI 추측" 배지로 실제 데이터와 구분
- 저장(POST/PATCH/DELETE)은 Mock 에서도 **상태를 실제로 바꾸고**, 재조회 반영까지 테스트합니다
- 화면 코드에 URL·snake_case·hex 색상을 쓰지 않습니다

---

## 부록 C. 대표 영상 / 가이드 영상 (2026-08-26)

AI 트렌드 클러스터가 챌린지마다 영상 주소를 **두 개** 갖고 있습니다
(`representative_youtube_url` / `guide_youtube_url`). 백엔드 PR
[skt-flyai-9th/backend#64](https://github.com/skt-flyai-9th/backend/pull/64) 로
`video_formats.reference_url`(대표) · `guide_video_url`(가이드) 두 필드가 됩니다.

프론트는 이렇게 씁니다 (`src/api/formatVideo.ts` 한 곳에서만 고릅니다).

| 화면 | 쓰는 주소 |
| --- | --- |
| 홈 피드 카드 · 관심 목록 | **대표** `reference_url` |
| 촬영 준비(5.2) · 카메라 PiP · 안무 카메라 | **가이드** `guide_video_url` |

### 확인 부탁드릴 것 두 가지

1. **9.1 `reference_video.reference_url` 에는 가이드 영상을 넣어 주세요.**
   명세에 "`video_formats.reference_url` 을 그대로 재사용" 이라고 돼 있는데,
   촬영 중에 따라 보는 영상이라 **가이드 영상**이 맞습니다. 대표 영상이 오면
   사장님이 따라 출 안무 대신 유행 소개 영상을 보게 됩니다.
   지금은 프론트가 값이 없을 때만 포맷의 가이드 영상으로 떨어뜨리고 있어,
   서버가 대표 영상을 보내면 그대로 대표가 재생됩니다.

2. **배지 4개 값이 전부 `null` 입니다.**
   `format_type` · `expected_duration_sec` · `shooting_difficulty` ·
   `face_exposure_level` — 트렌드 클러스터에 없는 값이라 백엔드도 비워 두고 있습니다.
   카드 아래 `#완성24초 #난이도하 #얼굴노출없음` 줄이 통째로 비어 나옵니다.
   AI 가 줄 수 있는 값인지, 아니면 사람이 채우는 값인지 알려 주세요.

---

## 부록 D. 실서버 실측 보고 (2026-08-26)

테스트 계정을 만들어 **실서버(`https://sarils.p-e.kr`)를 직접 호출해** 확인한 것들입니다.
추측이 아니라 전부 실제 응답이고, 요청·상태코드를 그대로 적었습니다.

```
계정      realsqa0826@test.co.kr  (user 25)
가게      스타벅스 한국프레스센터점 (store 21, 2.1 검색 결과로 등록)
프로젝트   34 · 35
```

### D-1. 🔴 7.1 기획 생성이 **항상 500** 입니다 — 촬영 흐름 전체가 막힙니다

```
POST /shorts-projects/34/plan   {"video_format_id": 5}   → 500 INTERNAL_ERROR
POST /shorts-projects/34/plan   {"video_format_id": 4}   → 500
POST /shorts-projects/35/plan   {"video_format_id": 4}   → 500   (새 프로젝트도 동일)
```

**이것 하나가 아래를 전부 무너뜨립니다.**

```
7.1 실패 → 프로젝트의 video_format_id 가 영영 null
        → 8.1 태스크 0개 (촬영할 컷이 안 생김)
        → 9.1 컷별 가이드 없음
        → 카메라 좌상단 참고 영상 창이 아예 안 뜸
```

사장님이 "촬영 화면에 참고 영상이 하나도 안 나온다" 고 하신 게 이 증상입니다.
프론트는 급한 대로 **사장님이 고른 포맷 번호를 카메라까지 직접 들고 가도록** 우회해
참고 영상만 살려 뒀습니다. 하지만 **컷 목록이 없는 건 우회가 안 됩니다.**

참고로 이 가게는 2.3 가져오기가 `IN_PROGRESS` 에서 멈춰 있고 메뉴가 0개입니다
(아래 D-4). 기획 생성이 메뉴를 필요로 해서 죽는 것인지, 별개의 버그인지 알려 주세요.

### D-2. 🔴 R06 AI 추천 — 대화는 되는데 **추천 생성이 503** 입니다

```
POST /stores/21/shortform-sessions        201  ✅  선택지 정상
POST /shortform-sessions/34/turns         200  ✅  ASK → SAVE_AND_ASK → CONFIRM 정상
   (메뉴 → 목적 → 촬영시간 → 얼굴노출 순으로 서버가 알아서 물어봅니다)
POST .../turns  {"input":{"type":"CONFIRM","value":true}}
                                          503  ❌  AI_SERVICE_UNAVAILABLE
                                                   (4초 간격 3회 재시도 전부 동일)
```

대화 자체는 훌륭하게 돕니다. **마지막 추천 생성만** AI 서버에 못 붙습니다.
사장님 화면에서는 "이대로 추천받기" 를 누르면 오류만 뜨고 끝납니다.

### D-3. 🟡 추천을 여러 개 주는데 **어느 것을 골랐는지 알릴 방법이 없습니다**

턴 응답의 키가 `recommendations` — **배열**입니다(추천 전에도 `[]` 로 옵니다).
시안도 카드 세 장을 가로로 넘기는 그림이라 배열이 맞습니다.

그런데 `POST /shortform-sessions/{id}/accept` 에는 **어느 추천인지 지정하는 자리가
없습니다.** 사장님이 첫 번째 카드를 눌렀는데 세 번째가 만들어질 수 있습니다.

프론트는 일단 `recommendation_id` 를 body 에 함께 보내고 있습니다.
**서버가 이 값을 보고 고르도록 해 주시거나, 다른 방법을 알려 주세요.**

### D-4. 🟡 추천 응답에 **영상·포맷 번호가 없습니다**

`recommendations[]` 가 주는 것은 `recommendation_id · project_title · title ·
concept · editing_template_id · editing_template_version` 입니다.
포맷 번호(`video_format_id`)는 **채택한 뒤에야** 응답으로 돌아옵니다.

그래서 시안의 추천 카드 안 **영상 자리(회색 상자)를 채울 수 없어 비워 뒀습니다.**
추천 단계에서 `video_format_id` 나 썸네일 주소를 함께 주실 수 있나요?

### D-5. 🟡 2.3 가져오기가 `PENDING` 에서 안 넘어갑니다

```
GET /stores/21/import-status
  overall_status: IN_PROGRESS
  기본정보 SUCCESS / 메뉴 PENDING / 사진 PENDING / 상권분석 PENDING
GET /stores/21/menus             → { "menus": [] }
GET /stores/21/target-customers  → { "target_customers": [] }
```

등록 직후부터 수십 분이 지나도 그대로였습니다. 네이버에서 가져온 유명 프랜차이즈
지점인데도 메뉴가 0개입니다. 이 상태면 "어떤 메뉴를 홍보할까요?" 화면에서 고를 것이
없습니다(프론트는 직접 입력으로 빠질 길을 열어 뒀습니다).

### D-6. 🟡 2.1 검색 응답에 `kakao_place_id` 가 **아예 없습니다**

```
GET /stores/search?keyword=스타벅스
  응답 키: source, name, address, phone, latitude, longitude,
           category, distance_m, external_channel_url
```

명세 2.1 에는 있다고 되어 있어 프론트가 2.2 등록 때 그대로 돌려보내도록 만들어
뒀는데, **받을 값이 없어 항상 null 로 나갑니다.** 대표메뉴 자동 수집 트리거가
동작하지 않을 텐데, 명세에서 빼는 게 맞는지 서버에 추가하실 건지 알려 주세요.

**좌표는 정상입니다** (`latitude: 37.5672475`, `longitude: 126.9780493`).
지도가 안 뜨던 건 좌표 문제가 아니라 타일 서버 쪽이었고, 프론트에서 해결했습니다.

### D-7. ⚪ 6.1 / 6.2 질문형 API 는 폐기된 것으로 이해했습니다

```
GET  /shorts-projects/34/quiz-questions   → 404 NOT_FOUND
POST /shorts-projects/34/quiz-answers     → 404 NOT_FOUND
```

R06 이 대화형 에이전트로 바뀌면서 없어진 것으로 보입니다. 맞다면 **명세에서
6.1·6.2·6.3 을 지워 주세요.** 프론트는 이 경로를 "서버에 아직 없는 것" 으로 보고
앱 안에서 흉내 내고 있었는데, 그 장치를 걷어내야 합니다.

### D-8. ⚪ `requires_face` 는 오는데 값이 전부 null 입니다

5.1 응답에 `requires_face` 가 있습니다(프론트 스키마에도 반영했습니다).
다만 20건 전부 `null` 이라 추천 카드의 `#얼굴미노출` 태그가 나오지 않습니다.
`format_type` · `expected_duration_sec` · `shooting_difficulty` 도 같습니다
(부록 C 2번과 같은 건입니다).

**반가운 소식 하나** — `guide_video_url` 이 실제로 들어와 있고 대표 영상과
**다른 주소**입니다. 부록 C 에서 요청드린 대로입니다. 감사합니다.

```
BAD 댄스 챌린지     대표 lYzMvH9CbLA  ≠  가이드 0Ez7Zjk1ehM
스파이더맨 챌린지    대표 TJswcVAYqps  ≠  가이드 qC3dbcu1vaU
```

### 우선순위

| | 항목 | 영향 |
|---|---|---|
| 1 | D-1 · 7.1 이 500 | 촬영 흐름 전체가 반쪽. 컷이 안 생겨 촬영을 시작할 수 없습니다 |
| 2 | D-2 · 추천 생성 503 | AI 추천 탭이 마지막 단계에서 끝납니다 |
| 3 | D-3 · accept 에 추천 지정 | 사장님이 고른 것과 다른 게 만들어질 수 있습니다 |
| 4 | D-5 · 가져오기 PENDING | 메뉴가 비어 홍보 대상을 고를 수 없습니다 |
| 5 | D-4 · 추천에 포맷 없음 | 카드 영상 자리를 못 채웁니다(시안과 다름) |
| 6 | D-6 · D-7 · D-8 | 명세 정리 · 데이터 채우기 |

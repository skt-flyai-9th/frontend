# 할 일 목록

**작성: 2026-08-26.** 시안 9차까지 대조하고, 실서버(`https://sarils.p-e.kr`)를
직접 호출해 막히는 지점을 확인한 뒤 정리한 것입니다.

작업은 **묶음 단위**로 합니다. 오늘 재보니 **빌드·검증이 코드 작성보다 오래
걸립니다** — `expo export` 한 번에 3~5분, 캡처 검증에 1~3분입니다. 그래서 같은
파일을 건드리는 것끼리 묶어 **빌드를 한 번만** 돌리는 쪽으로 짰습니다.

```
A → B → D → C → F → G → E
```

| 묶음 | 내용 | 예상 |
|---|---|---|
| **A** | 지금 틀린 값을 저장하는 것들 (업종 분류 · 저장 3건 · 화면 이름) | ~35분 |
| **B** | 매장 메뉴 관리 | ~70분 |
| **D** | 구간 반복 — 플레이어 쪽 (BE 값 오기 전에 미리) | ~30분 |
| **C** | 편집 로딩 애니메이션 (9차 신규) | ~60분 |
| **F** | 마이페이지 내 영상 자동재생 | ~40분 |
| **G** | 안무 확인 화면 | ~55분 |
| **E** | 인트로 삽화 4장 교체 | ~85분 |

전부 하면 **6시간 안팎**입니다. 중간에 사장님 확인이 끼면 더 걸립니다.

---

## A. 지금 틀린 값을 저장하는 것들 — **가장 먼저** (~35분)

다섯 개가 거의 같은 파일이라 **빌드 한 번에 다 검증**됩니다.
그리고 A-1·A-3 은 "화면이 사장님께 거짓을 말하는" 종류라 미루면 안 됩니다.

### A-1. 🔴 업종 분류를 **키워드 대조**로 바꾸기

`src/lib/format.ts` · `src/domains/store/screens/StoreSearchScreen.tsx`

지금 `shortCategory()` 는 **맨 뒤 조각만 떼는** 방식입니다. 실서버에서 서로 다른
카테고리 원문 59종을 뽑아 돌려 보니 두 가지로 깨집니다.

```
① 맨 뒤가 업종이 아니라 브랜드명
   가정,생활 > 미용 > 미용실 > 준오헤어   → "준오헤어"  (미용실이어야 함)
   ... > 드럭스토어 > 올리브영            → "올리브영"
   ... > 헬스클럽 > 스포애니              → "스포애니"

② 쉼표 뒤를 자르면 뜻이 뒤집힘
   음식점>카페,디저트   → "디저트"   ← 카페인데 디저트로 저장됩니다
   음식점>치킨,닭강정   → "닭강정"   ← 치킨집인데
   생활,편의>꽃집,꽃배달 → "꽃배달"
```

쉼표가 **목록**일 때도(`가정,생활`) **한 덩어리 이름**일 때도(`카페,디저트`) 있어
기계적으로 자를 수 없습니다.

**바꿀 방향** — 예쁜 라벨을 만들려 하지 말고, 경로 **전체**를 키워드로 훑어
**우리 칩 10개 중 하나**로 보냅니다. 같은 59종에 미리 돌려 본 결과입니다.

```
음식점>카페,디저트            → 카페      (꼬리 자르기는 "디저트")
가정,생활>미용>미용실>준오헤어  → 미용      (꼬리 자르기는 "준오헤어")
스포츠,레저>...>스포애니       → 운동

47종 중 38종 자동 분류 = 81%
```

남는 9종(약국·편의점·세탁소·드럭스토어·기업)은 **우리 칩에 없는 업종**이라
`직접입력` 으로 빠지는 게 맞습니다. 그 칸에 원문을 미리 채워 두면 사장님이
지우고 쓸 필요가 없습니다.

> ⚠️ **판단 필요:** 칩 목록 10개(시안 `SYNC_CATEGORIES`)를 유지할지, 약국·편의점
> 같은 걸 더 넣을지. 지금은 시안을 따르고 있습니다.

**왜 급한가:** 이 코드는 **이미 매장 등록에 들어가 있습니다.** 스타벅스로 테스트할
때 우연히 맞았을 뿐이고, 저장된 업종은 AI 추천의 입력으로 그대로 들어갑니다.

### A-2. 저장하면 **이전 화면으로 돌아가기**

`src/domains/my/screens/EditProfileScreen.tsx`

시안은 6차부터 이렇습니다. 확인 표시가 아예 없고 **화면이 닫히는 것 자체가 확인**입니다.

```js
const save = () => {
  updateStore({...});
  navigate("mypage");   // ← 시안
};
```

우리는 "저장했습니다" 배너를 띄우고 그 자리에 머뭅니다. 배너를 빼고 복귀로 바꿉니다.

### A-3. 🔴 저장이 **실패해도 "저장했습니다"** 가 뜨는 것

`src/domains/my/screens/EditProfileScreen.tsx`

```ts
updateStore.mutate({...});
setSaved(true);        // ← 응답을 안 기다립니다
```

`updateStore.isError` 를 화면 어디서도 보지 않습니다. **PATCH 가 실패해도 성공했다고
말합니다.** 사장님은 저장된 줄 알고 나갑니다 — 제1규칙 위반입니다.
`onSuccess` 에서만 넘어가고, 실패는 Banner 로 보여야 합니다.

> A-2 와 함께 고칩니다. 복귀도 **성공했을 때만** 해야 합니다.

### A-4. SNS 연동 **실패 얼럿**

`src/domains/my/screens/EditProfileScreen.tsx` · 시안 8차 신규

지금은 Banner 로만 알립니다. 시안은 화면 정중앙 `alertdialog` 입니다.

```
        ⚠
  {플랫폼} 계정이
  연동되지 않았어요
  다시 시도해 주세요.

  [ 닫기 ]  [ 다시 시도 ]
```

**오늘 인스타 연동이 실제로 실패하는 자리입니다**(BE §2-8). 안내가 약하면
사장님은 앱이 고장난 줄 압니다.

### A-5. 화면 이름 "프로필 수정" → **"매장 정보 수정"**

`EditProfileScreen.tsx` · 네비게이션 제목

매장 정보를 고치는 화면이라 시안 이름이 맞습니다. 5분짜리라 이 묶음에 넣습니다.

---

## B. 매장 메뉴 관리 (~70분)

`src/domains/my/screens/EditProfileScreen.tsx` + 신규 컴포넌트 · `api/queries/store.ts`

시안 8차의 매장 정보 수정에는 **"매장 메뉴 관리 · 총 N개"** 목록이 있는데
**우리 화면에는 메뉴가 아예 없습니다**(`grep 메뉴` 결과 0건).

서버 API 는 이미 다 있습니다.

```
GET    /stores/{id}/menus
POST   /stores/{id}/menus
PATCH  /stores/{id}/menus/{menu_id}
DELETE /stores/{id}/menus/{menu_id}
```

**왜 두 번째로 급한가:** AI 추천 대화가 "어떤 메뉴를 홍보할까요?" 를 묻는데,
서버 자동 수집(2.3)이 **지금도 `PENDING` 이라 메뉴가 0개**입니다. 사장님이 직접
넣는 길이 **유일한 해결**입니다.

```
GET /stores/21/import-status → 메뉴 PENDING / 사진 PENDING / 상권분석 PENDING
GET /stores/21/menus         → { "menus": [] }
```

⚠️ 매장 **등록** 화면에서는 메뉴를 못 채웁니다 — 2.1 검색 응답에 메뉴가 없고
`/stores/{id}/menus` 는 매장이 만들어진 **뒤**에야 부를 수 있습니다. 그래서
등록 2단계 문구를 "업종을 확인해 주세요" 로 적어 뒀습니다.

---

## D. 구간 반복 — 플레이어 쪽 (~30분)

`src/ui/guidePlayerBridge.ts` · `src/ui/GuidePlayer.tsx`

촬영 화면의 참고 영상을 **지금 찍을 컷의 구간만** 반복 재생합니다.

**기술 검증은 끝났습니다** (2026-08-26 실측). 실제 유튜브 임베드에 우리 플레이어와
같은 방식(IFrame API postMessage)으로 걸어 봤습니다.

```
컷1  구간  1.0~3.0s  → 실제 1.00 ~ 3.10   벗어남 0회
컷2  구간  5.0~8.0s  → 실제 5.00 ~ 8.18   벗어남 0회
컷3  구간  9.0~11.5s → 실제 9.00 ~ 11.65  벗어남 0회
컷 전환 중 iframe 재로딩 0회 (검은 화면 없음)
```

- 끝점을 0.1~0.2초 지나쳤다 되감깁니다(유튜브가 위치를 ~0.25초 간격으로 알려줌).
  더 줄일 방법은 없고, 3초 컷에서도 거슬리지 않는 수준입니다.
- 예전에 뺐던 방식(`20edec3`)은 문서 안 `<video>` 를 직접 잡다가 유튜브가 그 노드를
  갈아끼워 먹통이 됐던 것입니다. 지금은 IFrame API 라 같은 문제가 없습니다.

**여기서는 플레이어가 `loopStart`·`loopEnd` 를 받도록만 만듭니다.** 값이 없으면
지금처럼 전체 재생이라 부작용이 없고, BE 값이 오는 날 **연결만** 하면 됩니다.

🔴 **컷마다 물리는 것(7b)은 BE 대기** — `start_ms`·`end_ms` 가 영상편집 DB 에는
있다고 들었는데 **API 로는 하나도 안 나옵니다**(BE §2-10).

---

## C. 편집 로딩 애니메이션 — 시안 9차 신규 (~60분)

`src/domains/edit/screens/RenderScreen.tsx`

**9차에서 바뀐 것은 이것 하나뿐입니다.** 8차와 전 파일 대조 결과
`screens-shoot.jsx` + 엔트리 html 만 다릅니다.

```
before  190px 9:16 회색 상자 + "촬영된 영상" + 자막칩 + 위치칩
after   212px 전체폭 rounded-32 상자 + 3.2초 루프
        클립 3개가 흔들 → 가운데로 합쳐짐 → 파란 블랍 → 이퀄라이저 → 재생 삼각형
```

자막칩("이 한 그릇, 30년입니다")과 위치칩도 **함께 사라집니다** — 상자가 통째로
교체됩니다.

⚠️ **함정 두 개** (CLAUDE.md §5-④, §5-④-1)
- 블랍이 width·height·borderRadius 를 바꿉니다 → **레이아웃 속성이라 네이티브
  드라이버를 못 씁니다.** 한 노드에 두 드라이버를 섞으면 RN 이 예외를 냅니다.
- `Animated.loop` 는 웹에서 한 바퀴만 돌고 멈춥니다. **다음 바퀴를 직접 걸어야**
  합니다 (`Marquee.tsx` 가 같은 방식으로 돼 있으니 그걸 보면 됩니다).
- 시계는 **하나로** 통일합니다. 값을 둘로 쪼개면 두 시계가 어긋납니다.

---

## F. 마이페이지 내 영상 자동재생 (~40분)

`src/domains/my/screens/MyPageScreen.tsx`

실기기 평가에서 나온 요청입니다. 썸네일은 이미 15.2 `cover_image_url` 로 뜨는데
**자동재생이 없습니다.**

홈 피드에 쓴 방식(보이는 장만 플레이어, 나머지는 썸네일)을 그대로 가져오면 됩니다.
유튜브 약관상 **한 화면에 자동재생 플레이어는 하나**라 그 규칙을 지켜야 합니다.

⚠️ 웹 QA 로는 확인이 안 됩니다(`GuidePlayer.web.tsx` 가 자리표시자) — **실기기 확인**입니다.

---

## G. 안무 확인 화면 (~55분)

신규 화면 + `src/navigation/RootNavigator.tsx`

시안에는 안무 영상과 **"안무 순서" 단계 목록**을 보고 촬영에 들어가는 화면이
따로 있습니다(`ChoreoScreen`). 우리는 카메라에서 바로 `DanceCamera` 로 넘어가서,
사장님이 **안무를 미리 보고 외울 자리가 없습니다.**

⚠️ **선행 확인:** 시안은 목업(`REALS.CHOREO_STEPS`)을 씁니다. **9.1 이 안무 순서를
실제로 주는지** 먼저 확인해야 합니다. 안 주면 BE 요청 항목이 하나 더 늘어납니다.

---

## E. 인트로 삽화 4장 교체 (~85분)

`src/domains/onboarding/components/TutorialArt.tsx`

시안 8차에서 삽화가 **추상 도형 → 실제 앱 화면 크롭**으로 전면 교체됐습니다.
앱 화면을 393px 로 그린 뒤 0.682배로 축소·크롭하고, 그 위에서 검색이 자동완성되고
선택되고 메뉴가 채워지는 과정이 루프로 돕니다. 전용 키프레임이 19개 붙습니다.

**슬라이드 문구 4개는 글자 하나 안 바뀌었습니다.**

⚠️ 시안에서 **인트로로 가는 길이 끊겨 있습니다** — `SCREEN_NAMES`·`SCREENS`·`DEPTH`
어디에도 `intro` 가 없고 스플래시가 곧장 `auth` 로 갑니다. 코드는 살아 있는데
도달할 수 없습니다. **의도인지 실수인지 시안 쪽 확인이 필요합니다.**

**가장 오래 걸리는데 사장님이 앱을 처음 켤 때 한 번 보는 화면**이라 맨 뒤에 뒀습니다.

---

## 🔴 BE 답을 기다리는 것 (앞의 작업과 별개로 진행)

자세한 내용과 근거는 `BE_전달사항.md` 에 있습니다.

| | 항목 | 막는 것 |
|---|---|---|
| §2-8 | **인스타 `client_id` 가 페이스북 App ID** | SNS 연동 자체가 안 됩니다 |
| §2-10 | **참고 영상 `start_ms`·`end_ms` 가 API 에 없음** | 구간 반복 7b |
| §1-1 | 2.3 가져오기 `PENDING` → 메뉴 0개 | 메뉴 자동 수집 (B 가 우회로) |
| §1-2 | 5.1 포맷이 3건 | 홈 피드가 세 편뿐 |
| §2-9 | 2.1 에 지번 주소 없음 | 매장 확인 시트의 한 줄 |
| §2-1 | 추천에 `video_format_id` 없음 | 추천 카드의 태그·영상 |

---

## 🔴 빌드가 필요한 것 (묶어서 한 번에)

지금까지 한 작업은 **전부 OTA 로 나갑니다.** 아래만 APK 재빌드가 필요합니다.

| 항목 | 왜 |
|---|---|
| 내보내기 진짜 공유 | `expo-sharing` — 새 네이티브 패키지 |
| 푸시 알림 | `expo-notifications` + `google-services.json` (Firebase 담당 미정) |
| 앱 아이콘 교체분 | 이미 대기 중 |

**하나씩 빌드하지 말고 셋을 묶어 한 번에** 합니다. `app.json` 의 `version` 도
그때 함께 올려야 합니다 (`runtimeVersion` 이 `appVersion` 정책이라 안 올리면
옛 APK 가 맞지 않는 JS 를 받아 죽습니다 — CLAUDE.md §7-1).

---

## 못 하는 것

| 항목 | 이유 |
|---|---|
| 홈 릴스 업로더 프로필 사진 | 채널 썸네일을 줄 API 가 없습니다. YouTube Data API 키가 필요하거나 BE 가 `channel_thumbnail_url` 을 내려줘야 합니다 |
| 아이디 찾기 · 비밀번호 재설정 화면 | 계정 복구 API 가 MVP 에 없습니다. 화면만 만들면 눌러도 아무 일이 안 나고 사장님이 계정에 영영 못 들어갑니다. 지금은 문의 메일로 보냅니다 |

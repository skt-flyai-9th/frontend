# 디자인 1차수정 적용 현황 + 누락 화면 (2026-08-24, v13)

기준: `Design_1차수정.zip` (tokens/guidelines/components/ui_kits) · `Reals_1차수정.html`

## 0) 정직한 범위 고지 (2026-08-24 저녁 갱신)

**v13 은 전면 재디자인이 아니었습니다.** 토큰이 같은 계보라 일치했을 뿐, 시안에서 실제로
가져온 건 폰트·로고▶·chat 아이콘 3건이었고, **버튼·칩·입력·헤더 크기와 홈 카드 구조는
기존(우리) 것을 우선**하고 있었습니다. "시안 우선" 지시에 따라 아래 §1.5 부터 시정합니다.

## 1) 결론 먼저

- **색·라디우스·그림자 토큰은 이미 일치합니다.** 1차수정의 `tokens/colors.css`·`layout.css` 값이
  v7~v8 에서 적용한 현재 `src/design/tokens.json` 과 동일합니다 (#2563EB, canvas 흰색,
  surface #F8FAFC, heart #EF4444, hairline #E2E8F0, radius 12/16/pill, 그림자 4종 동일 수치).
- 이번에 **새로 반영한 것**: ① Pretendard 폰트(진짜 OTF 4종 이식, 전 화면 적용),
  ② Reals 로고의 빨간 ▶(홈 헤더), ③ AI추천 탭 아이콘 chat 말풍선으로 교체.
- ~~의도적으로 다르게 유지~~ → **철회 (2026-08-24 "시안 우선" 지시).**

## 1.5) 시안 우선 전환 — v14 에서 적용 완료

| 항목 | 시안 | v13까지 | v14 |
|---|---|---|---|
| 버튼 높이/글자 | 48 / 15·600 | 58 / 19·700 | **48 / 15·600** ✅ |
| 입력 높이 | 52 | 58 | **52** ✅ |
| 칩 | 32 / 13 | 44 / 15 | **32 / 13** ✅ (hitSlop 으로 터치 44 보전) |
| 헤더 행 | 44 | 56 | **44** ✅ |
| 보조 아이콘 버튼 | 36 | 44 | **36** ✅ (hitSlop 보전) |
| 본문 | 15 | 17 | **17 유지** — 40~60대 가독 하한. 시안 전환 원하시면 한 줄 변경입니다 |

## 1.6) 시안 구조 교체 진행표 (v15 갱신)

| # | 항목 | 상태 |
|---|---|---|
| 1 | **TopHeader** — BlurView+흰0.95, 하단 rgba(226,232,240,.6), 행44, 타이틀 18·700 절대중앙, back 36/chevron24, logo variant(Reals▶) | ✅ v15 (`src/ui/AppBar.tsx` 전면 재작성) |
| 2 | **홈/관심 ReelCard 구조** — 풀블리드(테두리·그림자 제거), 4:5, 제목 15·600, 해시태그 12·500, 액션 36 | ✅ v15 (`FormatCard.tsx`, 리스트 패딩 제거) — 아바타·채널명은 5.1 에 필드 없음 → 생략, BE 질문 |
| 3 | **FeasibilityBadges** — 캡슐 11·600, 아이콘13 | ✅ v15 신규 `src/ui/FeasibilityBadges.tsx`, FormatDetail 적용(약관상 플레이어 아래) |
| 4 | **splash** — Reals▶ 중앙 | ✅ v15 (App.tsx 폰트 게이트 = 시안 splash) |
| 5 | 홈 헤더 AppBar(logo) 통일 — v13 로고▶가 홈에 미적용이던 것 시정 | ✅ v15 |
| 6 | 화면 순회 대조 | 🔶 v16 추가분: **chat 추천카드**(brand-tint+brand-border → QuizResult), **camera 크롬**(셔터 76 링, 녹화 중 링 빨강 — Camera·DanceCamera), states 는 기존 EmptyState 패턴 일치 확인(스켈레톤은 §3 모션과 함께). 남은 것: shoot-topic·store-sync·choreo (기획 확인 대기 §5) |
| 7 | legal 목록 화면 | ✅ v16 `LegalScreen` 신규 — 설정 '약관 및 정책' → 목록 → Terms 읽기모드 |
| 8 | 모션 | 🔶 v16 1차: whileTap 스케일(아이콘 0.9·버튼 0.98·셔터 0.94) 이식. 탭 스와이프 페이저·화면전환 스프링은 §3 잔여 |

## 2) 이번 v13 반영 상세

| 항목 | 디자인 사양 | 반영 위치 |
|---|---|---|
| Pretendard | fonts.css, OTF self-host | `assets/fonts/*.otf` + `src/design/fonts.ts` `USE_CUSTOM_FONTS=true` |
| 로고 ▶ | RealsLogo.jsx (size×0.31, baseline, heart색) | `HomeFeedScreen` logoRow/logoPlay |
| 탭 AI추천 | TabGlyph 'chat' | `RootNavigator` MessageCircle |
| 탭바 26px·하트 빨강·32×4 인디케이터 | TabBar.jsx | **v7 에서 기적용** — 일치 확인 |

## 3) 아직 반영하지 않은 것 (다음 차수)

| 항목 | 사양 출처 | 미룬 이유 |
|---|---|---|
| 탭 스와이프 페이저 + 탄성 캡슐 보간 | motion.css, TabPager.jsx (spring 350/32, 제스처 콘 30°) | react-native-pager-view 도입 + 4탭 구조 변경 — 별도 작업 단위 |
| 화면 전환 스프링(push 420/40 등) | motion.css | 내비게이터 전환 커스터마이징, 저사양 기기 검증 필요 |
| 시트 radius 28 / 다이얼로그 24 | layout.css | 현재 앱에 바텀시트·센터다이얼로그 컴포넌트가 없음 — 생기는 화면에서 적용 |
| Pretendard-Black(900) | fonts.css | 현재 타이포 스케일에 900 사용처 없음 — 파일만 보류 |

## 4) 화면 대조 — 디자인에 있는데 우리 앱에 없는 것

디자인 `components/screens/` 24종 ↔ 앱 46종 대조 결과:

| 디자인 화면 | 앱 대응 | 판정 |
|---|---|---|
| `splash` | 없음 | **누락.** 앱 시작 스플래시(로고+▶). app.json splash 는 정적 이미지라 별개 |
| `account-recovery` | 없음(의도) | 비번 재설정은 **Phase 2 확정**(BE 2026-08-24) — 로그인 화면 이메일 안내로 대체 중. 디자인만 선행된 상태 |
| `choreo` | 없음 | **확인 필요.** dance-camera(=DanceCamera 有)와 별도 파일 — 안무 "고르기/미리보기" 화면으로 추정. 기능명세에 대응 요구 없음 → 기획 확인 후 결정 |
| `legal` | Terms 로 부분 대응 | 디자인은 약관 "목록" 화면, 우리는 개별 약관 뷰어. 목록 껍데기 **누락** |
| `store-sync` | StoreSearch+StoreConfirm 으로 분할 대응 | 디자인은 단일 "가게 연동" 화면 — 구조 다름, 통합 여부 확인 필요 |
| `editing` | Render(진행)+EditResult 로 대응 | 디자인은 편집 진행을 단계 애니메이션으로 — 모션만 미반영 |
| 나머지 18종 (auth·camera·chat·export·faq·format·home·insight·my-video·mypage·permissions·plans·saved·settings·shoot-topic·signup·edit-profile·dance-camera) | 전부 존재 | 구조 일치 — 토큰·폰트 반영으로 자동 갱신됨 |

**앱에만 있는 화면**(디자인 미제공, 유지): PurposeSelect·PromotionDetail·PlanSummary·Storyboard·TaskBoard·TaskGuide·TakeReview·Evaluation·SubtitleEdit·Outputs·Publish·PostLink·Performance·Quiz(+Result)·Store 관리 4종·Target 2종·PathChoice·ShootCondition·StoreManual·Intro — 기능명세 기반 화면들. 1차수정 범위 밖으로 보이며, 토큰·폰트는 동일하게 적용됨.

## 5) 요청 필요 (기획/디자인)

1. `choreo` 화면의 정체 — 안무 선택인지, DanceCamera 진입 전 미리보기인지
2. `store-sync` 단일화 여부 — 현행 2단계(검색→확인) 유지해도 되는지
3. `splash` 노출 시간·조건 (즉시 홈 이동 vs 로딩 게이트)

/**
 * ⏳ **녹화용 임시 값** (2026-08-31).
 *
 * 화면 녹화를 찍는 동안만 쓰는 값입니다. **끝나면 이 파일과 이 파일을 부르는
 * 자리를 지우세요.** 지금 이 파일을 참조하는 곳은 아래 하나뿐입니다.
 *
 *   `domains/my/screens/MyPageScreen.tsx` — 프로필 위 `Views` 통계
 *   `domains/my/screens/InsightScreen.tsx`  — "다음 숏폼 추천" 카드의 영상
 *   `domains/my/screens/InsightScreen.tsx`  — 증감 배지 감추기 (차트 · 카드 둘 다)
 *
 * 왜 여기 모아 두는가 — 이 값들은 **서버가 주지 않는 숫자**입니다. 화면에
 * 직접 박아 두면 나중에 "이건 진짜 데이터인가" 를 가릴 수가 없습니다.
 * 이 프로젝트 제1규칙(없는 데이터를 지어내지 않는다)을 **일부러, 한시적으로**
 * 어기는 자리라 한 파일에 몰아 두고 이름에 그 사실을 적어 둡니다.
 *
 * 되돌리는 법은 둘 중 아무거나.
 *   ① 아래 `DEMO_RECORDING` 을 `false` 로
 *   ② 이 파일과 `MyPageScreen` 의 import·사용 한 줄을 지우기
 *
 * ⚠️ 나머지 녹화용 값(연동 계정 이름·주간 지표)은 **mock 데이터**라 여기 없습니다.
 *    `api/mock/server.ts` 의 `CONNECTIONS_SEED` 와 `api/mock/fixtures.ts` 의
 *    `weeklySummary` 에 `⏳ 녹화용` 으로 표시해 두었습니다. 그쪽은 mock 이므로
 *    실서버로 붙으면 저절로 원래 값으로 돌아갑니다.
 */
export const DEMO_RECORDING = true;

/**
 * 마이페이지 프로필의 `Views`.
 *
 * 계정 단위 누적 조회수 API 가 **없습니다**(17.1 은 게시물 단위). 그래서 평소에는
 * `—` 로 둡니다 — 0 으로 채우면 "실제로 0" 이라는 거짓말이 됩니다.
 * 녹화에서는 인사이트의 주간 조회수와 같은 값을 보여 줍니다.
 */
export const DEMO_ACCOUNT_VIEWS = '1,726';

/**
 * "다음 숏폼 추천" 카드에 깔 영상.
 *
 * 3.5 insights 는 제목·본문만 주고 **영상은 주지 않습니다.** 그래서 평소에는
 * 회색 판만 있습니다. 녹화에서는 예시로 보여 줄 게 필요해 실재하는 쇼츠 하나를
 * 깝니다 — 가짜 도메인을 쓰면 실기기에서 빈 칸으로 뜹니다.
 *
 * ⚠️ 녹화에서 **직접 찍는 숏폼과 겹치지 않는 것**으로 고릅니다 (2026-08-31 지시).
 *    주술회전 트랜지션(`Aa-CGr9-c8E`)은 그래서 뺐습니다.
 */
export const DEMO_REC_VIDEO_URL = 'https://www.youtube.com/shorts/rUIEHnyoPrU';

/**
 * **증감 배지를 전부 감춥니다** (2026-08-31 지시: "그 조회수랑 좋아요 수 증감표
 * 빼줘" → "조회수 추이 차트에도 안 뜨게").
 *
 * 감추는 곳 셋 — 주간 조회수 추이 오른쪽 위 · 총 조회수 카드 · 좋아요 수 카드.
 * 숫자만 크게 남는 편이 녹화에서 읽기 좋습니다.
 *
 * 계속 이렇게 가고 싶으면 이 값을 지우고 화면에서 그 세 줄을 아예 빼면 됩니다.
 */
export const DEMO_HIDE_KPI_DELTA = true;
